# 자동시험 전략 v1.2

## 1. 자동화 범위

| 계층 | 도구(권장, 스택 확정 후 조정) | 대상 |
|---|---|---|
| 백엔드 단위/통합 시험 | 언어별 표준 프레임워크(예: JUnit/pytest/Jest) | 의도매칭, 슬롯필링, API연동 로직 |
| 대화 품질 회귀 시험 | No.19 "대화검증시스템&TC테스트" 기능을 CI에서 헤드리스로 실행 | 엑셀 시드 데이터 기반 인텐트/엔티티/응답 매칭 |
| E2E/UI 시험 | Playwright 또는 동급 도구 | 관리 콘솔 주요 플로우, 챗봇 위젯 대화 흐름 |
| 접근성 자동 검사 | axe-core 등 정적 접근성 스캐너 | 색상대비, ARIA 속성, 폼 레이블 누락 등 자동 검출 가능 항목 |
| 부하 시험 | k6 또는 동급 도구 | API 게이트웨이/대화 엔진 P95 지연, 동시세션 |
| 보안 정적분석 | SAST 도구(스택 확정 후 선정) | 인증/인가 로직, 의존성 취약점 |

## 2. CI/CD 연계

- PR 생성 시: 단위/통합 시험 + 정적분석 자동 실행 (`docs/05-ops/자동배포.md`의 파이프라인 1단계와 연계)
- main 병합 전: 대화 품질 회귀 시험 + E2E 핵심 플로우 실행, 실패 시 병합 차단
- 배포(스테이징) 후: 부하 시험 + 접근성 자동 검사 실행, 결과를 `오류검출_프로세스.md` 기준으로 트리아지
- **현재 상태(2026-09-19)**: CI 워크플로 자체는 아직 미구성(`docs/05-ops/자동배포.md` §1에 따라 사용자가 플랫폼/자격증명을 명시하기 전까지 착수하지 않음). 아래 §3에 실제 구현된 로컬 실행 스크립트만 존재한다.

## 3. 실제 구현 현황 — 챗봇 운영관리(No.1~4)

> 이 그룹(No.1~4)은 1차 개발 첫 버티컬 슬라이스로, 아래 스택이 실제 코드로 존재한다. 다른 기능그룹(No.5~) 착수 시 동일 컨벤션을 재사용한다.

### 3.1 `apps/api` — Jest

| 계층 | 위치 | 실행 |
|---|---|---|
| 단위(순수 함수) | `apps/api/src/**/lib/*.spec.ts`(6개 파일: slug/skin/copy-name/status-transition/dashboard-aggregator/dashboard-period) | `pnpm --filter @chat-bot/api test` |
| 통합(HTTP 계약 테스트) | `apps/api/src/integration/chatbot-operations.integration.spec.ts` — 임시 SQLite DB(`prisma db push`)에 실제 NestJS 앱을 기동해 상태코드/오류코드/zod 스키마 정합성 검증 | 〃(동일 커맨드로 함께 실행) |

- 통합 테스트는 `docs/requirements/chatbot-operations.md` §7 AC 전체 중 순수 함수로 커버되지 않는 HTTP 레벨 항목(AC-1-1~1-16, AC-2-5/2-9/2-10, AC-3-1~3-5, AC-4-2/4-4/4-5/4-7/4-9)을 다룬다. 세부 매핑은 `시험항목.md`의 "챗봇 운영관리 상세 시험항목" 표 참고.
- 신규 AC/EX가 추가되면 **기존 파일을 지우지 말고 이 통합 스펙에 `it(...)` 블록만 추가**한다(테스트 실행 시간 단축 목적 — NestJS 앱 부팅이 `beforeAll`에서 1회만 발생).

### 3.2 `apps/web` — Vitest + Testing Library + jest-axe

- 2026-09-19 기준 신규 도입. `package.json`에 `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom`, `jest-axe`, `@types/jest-axe`를 devDependency로 추가했다(`pnpm --filter @chat-bot/web add -D ...`).
- 설정: `apps/web/vite.config.ts`의 `test` 필드(`environment: 'jsdom'`, `setupFiles: ['./src/test/setup.ts']`, `globals: false`). `globals: false`를 유지하므로 각 스펙 파일은 `vitest`에서 `describe/it/expect/vi`를 명시적으로 import한다(프로젝트의 명시적 import 컨벤션 유지).
- `src/test/setup.ts`: `@testing-library/jest-dom` matcher 등록, 각 테스트 후 `cleanup()` 명시 호출(globals=false라 testing-library 자동 cleanup 훅이 등록되지 않아 수동 등록 필요), jsdom `offsetParent` 폴리필(jsdom은 레이아웃 엔진이 없어 모든 요소의 `offsetParent`가 항상 `null`이다 — `components/Modal.tsx`의 포커스 트랩이 `offsetParent !== null`로 "보이는 요소"를 판별하므로 이 폴리필이 없으면 Tab 트랩 시험이 실패한다. **앱 코드 버그 아님, 테스트 환경 한계에 대한 테스트 전용 보정**).
- **주의**: `vitest.config`의 `restoreMocks`/`clearMocks` 옵션은 켜지 않는다. 이 저장소의 spec 파일들은 대부분 모듈 스코프에서 한 번 `vi.fn().mockResolvedValue(...)`를 설정하는 패턴을 쓰므로, 저 옵션을 켜면 매 테스트 전에 mock 구현이 초기화되어 `Cannot read properties of undefined (reading 'then')`류 오류가 발생한다. 각 spec이 필요하면 `beforeEach`에서 명시적으로 `mockReset()`/재설정한다.
- 실행: `pnpm --filter @chat-bot/web test`(= `vitest run`), 타입체크는 `pnpm --filter @chat-bot/web typecheck`.

| 대상 | 파일 | 검증 항목 |
|---|---|---|
| 모달 포커스 트랩/Esc/포커스복귀 | `src/components/Modal.spec.tsx` | AC-5-5, UIUX §3 |
| TopBar 저장 안 된 변경사항 가드 | `src/components/TopBar.spec.tsx` | AC-3-8 |
| slug 변경 경고 모달 | `src/pages/chatbot-detail/SettingsTab.spec.tsx` | AC-3-7, AC-5-5 |
| 대시보드 빈 상태/로딩/새로고침 연타 | `src/pages/chatbot-detail/DashboardTab.spec.tsx` | AC-2-6, AC-2-11, AC-2-12 |
| 그룹/챗봇 CRUD 플로우 + 키보드 전용 플로우 | `src/pages/ChatbotListPage.spec.tsx` | S-1, EX-1-3, EX-1-5, AC-5-3 |
| axe 접근성 스캔 | `src/pages/ChatbotListPage.a11y.spec.tsx` | AC-5-4(TC-A2) — `color-contrast` 룰은 jsdom 한계로 비활성화, 별도 단위시험으로 보완 |
| 명도대비 계산(WCAG) | `src/lib/contrast.spec.ts` | FR-4-6, AC-4-3 |
| 대비 경고 배지 렌더 조건 | `src/pages/chatbot-detail/ContrastWarningBadge.spec.tsx` | AC-4-3 |

- **미자동화(수동 확인 또는 후속 과제로 명시)**: AC-4-1(실시간 미리보기 반영), AC-4-8/EX-4-2(클립보드 복사 성공/폴백), AC-4-10(DRAFT 임베드 주의문구), AC-4-11(headerTitle XSS 미실행 — React 기본 이스케이프에 의존), AC-5-3의 전체 화면 종단간(그룹트리→설정폼저장) 키보드 플로우. 상세 사유는 `시험항목.md` 매핑 표 참고.

### 3.3 axe-core(jest-axe) 관련 알려진 한계

jsdom에는 실제 CSS 레이아웃/렌더링 엔진이 없어 `color-contrast` 규칙이 배경색 합성을 계산하지 못하고 "incomplete"만 반환한다. 따라서 `ChatbotListPage.a11y.spec.tsx`에서는 이 룰만 비활성화하고, 나머지 axe 규칙(레이블 누락, ARIA 속성, 랜드마크, 중복 id 등)은 그대로 적용한다. **실 브라우저 렌더링 기반 대비 자동 스캔은 Playwright + `@axe-core/playwright` 등 E2E 인프라 도입 시점에 보완이 필요하다** — 우선순위는 §4 "다음 자동화 대상"에 기록.

### 3.4 실제 구현 현황 — 대화 설계(No.5~9), 2026-09-20 시험 진행

> 대상 문서: `docs/requirements/dialogue-design.md` §7(AC)/§8(EX). 요구사항→설계→구현→code-reviewer(H1/H2/H3/M2 재작업 완료)까지 끝난 상태에서 시험을 진행했다. 세부 AC/EX ↔ 자동화 위치 매핑은 `시험항목.md`의 "대화 설계(No.5~9) 상세 시험항목" 표를 참고.

**`apps/api` — Jest(§3.1과 동일 컨벤션, 신규 파일만 추가)**

| 계층 | 위치 | 비고 |
|---|---|---|
| 통합(HTTP 계약) | `apps/api/src/integration/dialogue-design.integration.spec.ts`(47개 `it`) | 의도/키워드/동음이의어/컨텍스트/노드/FAQ 6개 모듈 + 대량업로드 2단계 + 공통/횡단(AC-C)을 커버. `chatbot-operations.integration.spec.ts`와 같은 부트스트랩 패턴(임시 SQLite + 실 NestJS 앱)을 재사용하되, 파일 업로드(`multipart/form-data`) 엔드포인트 호출을 위해 Node 18+ 내장 `fetch`+`FormData`+`Blob`로 별도 `uploadCsv()` 헬퍼를 추가했다(신규 의존성 없음). |
| 단위(순수 함수) | 기존 `import-planner.spec.ts`(M2) 등 6개 파일 — 변경 없음 | 그대로 유지, 삭제/재작성하지 않음 |

- **H2 검증(핵심)**: `EX-R-2`(의도 삭제가 동음이의어 참조로 차단, 메시지에 "동음이의어" 포함)와 `EX-R-3`(키워드 삭제가 컨텍스트 슬롯 참조로 차단, 메시지에 "컨텍스트" 포함)를 통합 테스트가 매 케이스마다 직접 만드는 조합으로 재현했다. seed(`apps/api/prisma/seed.ts`)에는 이 조합이 없어 frontend-implementer가 자체 미검증으로 보고했던 지점이다 — `시험데이터.md` §9.2 참고.
- **M2 통합 검증**: 키워드 대량업로드 동의어 충돌 시나리오(묶음 전체 스킵)를 실제 `import/validate` → `import/commit` HTTP 왕복으로 재현해, 검증 리포트의 예고(`errors[]` 2건)와 커밋 결과(`createdItems:0, skippedRows:2`)가 정확히 일치함을 확인했다(순수 함수 단위시험 `import-planner.spec.ts`는 이미 있었으나 HTTP 계약 레벨 검증은 없었다).
- **H3 검증**: `dialog-nodes`의 `nodeType` 다중 필터가 서버 `csvEnumArray`로 정상 파싱되어 지정한 타입만 반환하는지 통합 테스트로 확인(프런트 위임 여부는 `apps/web`에서 별도 검증, 아래).

**`packages/dialogue-engine` — Jest(M4 보강)**

| 파일 | 내용 |
|---|---|
| `src/resolver.performance.spec.ts`(신규) | code-reviewer M4 지적사항(성능 테스트 공백) 보강. 기존 `resolver.spec.ts`의 `AC-E-12` 케이스(의도 200×예문20/FAQ 300/노드 0, 100회 총합 2초 기준)는 그대로 두고, ADR-0008/FR-E-10이 명시한 실제 설계 규모(의도 1,000×예문 20,000 / 노드 500 / FAQ 2,000)를 재현하는 별도 파일을 추가했다. `buildDialogueIndex` 재사용 시 개별 호출 P95 < 200ms(NFR-P3)를 검증하고, 노드매칭/FAQ매칭/완전폴백 3개 경로를 섞어 측정한다. `@types/node` 미의존 정책(NFR-M1) 때문에 `process.hrtime`/`console` 대신 `Date.now()`만 사용한다. |

**`apps/web` — Vitest+Testing Library(§3.2와 동일 컨벤션, 신규 파일 6개)**

| 대상 | 파일 | 검증 항목 |
|---|---|---|
| 삭제차단 배너 3종(H1/H2 핵심) | `src/pages/dialogue/components/DeleteBlockedBanner.spec.tsx` | `resolveBlockedRefKind` 판별 로직(5케이스) + node/homonym/context 3가지 kind의 라벨·href·클릭 시 이동(9개 `it`) |
| 순서변경 키보드 조작 | `src/components/ReorderableList.spec.tsx` | UIUX §3(드래그 금지), AC-5-8(포커스 유지), 클릭/키보드 동일 동작 |
| 아웃풋 타입 전환 | `src/pages/dialogue/components/DialogOutputEditor.spec.tsx` | TEXT→CARD→IMAGE→BUTTON→PAUSE→SURVEY 전환 시 서브폼 교체 + 포커스 이동 + 미지원 배지(FR-5-15) |
| 대량 업로드 3단계 | `src/pages/dialogue/components/BulkImportModal.spec.tsx` | 파일선택→검증→확정 플로우, 정책 라디오, 토큰만료/전체취소 배너, 중복제출 방지 |
| 노드 목록(H3 프런트 측) | `src/pages/dialogue/NodesListPage.spec.tsx` | 다중유형 필터가 클라이언트에서 걸러내지 않고 서버 호출 파라미터로 그대로 위임되는지, 흐름미리보기/설계점검 패널 렌더, 노드 삭제차단(kind=node) |
| 의도·키워드 CRUD + H2 프런트 측 | `src/pages/dialogue/IntentsKeywordsPage.spec.tsx` | 의도/키워드 생성 플로우, 탭 전환, 삭제차단 배너 kind 매핑 종단 검증 |

- **미자동화(수동 확인 또는 후속 과제로 명시)**: 컨텍스트 폼 편집(`ContextFormPage`)·동음이의어 편집(`HomonymEditModal`)·FAQ 편집(`FaqEditModal`)·노드 편집 폼 전체 플로우(`NodeFormPage`, `ResourcePickerField` 네트워크 의존)는 이번 시험에서 다루지 않았다 — 다음 우선순위로 §4.2에 기록.

### 3.5 실제 구현 현황 — 품질/채널(No.10~11), 2026-09-20 시험 진행

> 대상 문서: `docs/requirements/quality-channel.md` §7(AC)/§8(EX). 요구사항→설계→백엔드→프런트엔드 구현과
> code-reviewer 리뷰(High 2건 — 오버레이 크기제한 통합테스트 부재/PII마스킹 문서정정, Medium 다수 — SIM1-D
> 3진입점 연결·위젯 ESLint가드 등 — 전부 재작업 완료)까지 끝난 상태에서 시험을 진행했다. 이 그룹에서
> **`apps/widget` 신규 앱**이 처음 추가되어 자동화 스택이 하나 더 늘었다(§3.5.3). 세부 AC/EX ↔ 자동화 위치
> 매핑은 `시험항목.md`의 "품질/채널(No.10~11) 상세 시험항목" 표를 참고.

#### 3.5.1 `apps/api` — Jest(§3.1과 동일 컨벤션)

| 계층 | 위치 | 비고 |
|---|---|---|
| 통합(HTTP 계약) | `apps/api/src/integration/quality-channel.integration.spec.ts`(29개 `it`) | 채널 CRUD, 시뮬레이션(오버레이/비교), 공개 대화 API, 노드 직접 점프, 동음이의어 되묻기 왕복을 커버. `chatbot-operations`/`dialogue-design`과 같은 부트스트랩 패턴(임시 SQLite + 실 NestJS 앱)을 재사용하되, **body-parser 상한을 `main.ts`와 동일하게 2MB로 맞추도록 부트스트랩을 보강**했다(아래 참고). |
| 단위(순수 함수) | `simulation/lib/compare-diff.spec.ts`, `conversation/lib/{pii-mask,origin-match,rate-limiter,conversation-log,output-degrade}.spec.ts`(6개 파일) | 신규 — 오버레이 A/B 비교 판정, PII 마스킹, Origin 검증, 레이트리밋 카운터, `isAnswered` 판정, 아웃풋 격하 로직 |

**code-reviewer 지적사항 보강 — 오버레이 요청 본문 크기(FR-10-21)**: 기존에는 100KB~2MB 구간의 실제 HTTP 페이로드 크기 테스트가 없어 수동 curl로만 검증되어 있었다. 이번에 다음 2건을 신규로 추가했다.

1. 약 200KB(과거 Express 기본 body-parser 상한인 100KB는 넘지만 오버레이 1MB 상한(`OVERLAY_LIMITS.bodyBytes`)보다는 작은 크기) → `200` 정상 처리.
2. 약 1.08MB(1MB 상한은 넘지만 부트스트랩에 설정한 2MB 하드 캡보다는 작은 크기) → `400 LIMIT_EXCEEDED`(Express의 생 `413`이 아니라 우리 오류 봉투).

이 두 테스트가 의미를 가지려면 **테스트용 Nest 앱도 `main.ts`와 동일하게 body-parser 상한을 2MB로 올려야 한다** — 기존 `Test.createTestingModule(...).compile()` → `createNestApplication()` 흐름은 Express 기본값(약 100KB)을 그대로 썼기 때문에, 고치지 않으면 정상 크기 오버레이 요청도 생 `413`으로 실패해 버그를 오히려 은폐하는 테스트가 됐을 것이다. `quality-channel.integration.spec.ts`의 `beforeAll`에 `app.useBodyParser('json', { limit: '2mb' })` 등을 `main.ts`와 동일하게 추가해 이 문제를 막았다. 크기 제어는 examples 배열(항목당 최대 200자, 의도당 최대 500개)을 ASCII로만 채워 문자 수(zod `.length` 기준)와 바이트 수(`Buffer.byteLength`, 실제 HTTP 크기)가 사실상 같아지도록 설계했다.

**추가로 발견해 보강한 커버리지 공백**:

- **AC-P-12(공개 API 레이트리밋 429)**: 기존에는 `rate-limiter.spec.ts`의 `consume()` 순수 함수만 테스트되어 있었고, `PublicRateLimitGuard`가 실제 컨트롤러에 배선되어 `429`+`Retry-After`를 반환하는지의 통합 테스트는 없었다. 같은 `sessionId`로 31회 연속 HTTP 요청을 보내 31번째가 `429 RATE_LIMITED`+`Retry-After` 헤더를 반환함을 확인하는 테스트를 추가했다.
- **AC-C-1(응답 스키마 계약)**: 이 그룹의 통합 테스트는 다른 두 그룹과 달리 성공 응답에 대한 zod 파싱 검증이 전혀 없었다(오류 응답의 `ApiErrorSchema.parse`만 있었음). 대표 성공 케이스 5건(AC-10-1/AC-11-1/FR-10-25~30/AC-P-1/AC-P-6)에 `SimulateResponseSchema`/`ChannelListItemSchema`/`CompareResponseSchema`/`PublicChatbotConfigSchema`/`PublicMessageResponseSchema.parse()`를 추가해 계약 검증 공백을 메웠다. 단, 이 스키마들은 `.strict()`가 아니라서 **여분 필드 추가**(AC-C-5, NFR-S1 회귀)는 zod가 조용히 strip하므로 런타임 계약 테스트로는 못 잡는다는 한계를 `시험항목.md`에 명시했다 — `.strict()` 전환 여부는 `system-architect` 판단 필요.

#### 3.5.2 `packages/dialogue-engine` — Jest(엔진 보강 FR-E2 검증)

| 파일 | 내용 |
|---|---|
| `src/turn.spec.ts`(신규) | `resolveByNodeId`(FR-E2-1) 5개 테스트, `resolveTurn` 오케스트레이터(버튼/텍스트 라우팅, 손상 state 폐기, 교차 챗봇 방어) 5개 테스트, 되묻기 종결(FR-E2-2, S1.5) 8개 테스트(버튼 클릭 정확 일치·문맥 힌트·불일치 폐기·TTL 만료·세션 우선·NODE 버튼 무시) |
| `src/conversation-state.spec.ts`(신규) | `ConversationState` 봉투 검증/정규화(버전·만료·손상 스키마 폐기) |
| `src/overlay.spec.ts`(신규) | `mergeOverlay`(FR-10-19) — id 교체/`draft-` 신규추가/삭제/원본 불변성 6개 테스트 |
| 기존 `resolver.spec.ts`/`matcher.spec.ts`/`homonym.spec.ts`/`design-validator.spec.ts`/`flow-tree.spec.ts`/`context-session.spec.ts`/`resolver.performance.spec.ts` | 변경 없음, 하위호환 확인(AC-E2-6) — 전체 72개 그대로 통과 |

#### 3.5.3 `apps/web` — Vitest+Testing Library(§3.2와 동일 컨벤션, 신규 파일 6개)

| 대상 | 파일 | 검증 항목 |
|---|---|---|
| 시뮬레이터 멀티턴 대화·되묻기·노드점프 | `src/pages/chatbot-detail/simulator/SimulatorPanel.spec.tsx` | AC-10-4/5(멀티턴 `state` 왕복), FR-E2-2/AC-E2-4(동음이의어 되묻기 버튼 클릭→해소를 화면에서 실제 클릭으로 검증), FR-E2-1(노드 검색→선택→직접 실행), FR-10-12(대화 초기화), AC-10B-6(오버레이 배지) — 5개 `it` |
| A/B 비교 diff 표시 | `src/pages/chatbot-detail/simulator/CompareView.spec.tsx` | AC-10B-7/8(변경된 행만 배지), FR-10-31(빈 오버레이 차단), "달라진 것만 보기" 필터(재요청 없음) — 3개 `it` |
| 채널 카드 aria-disabled | `src/pages/chatbot-detail/channels/ChannelCard.spec.tsx` | AC-11-12/NFR-A3(`CONFIG_ONLY`는 `aria-disabled`+사유, WEB은 잠기지 않음), FR-0-22(ARCHIVED 잠금), 409 오류 토스트 — 4개 `it` |
| SIM1-D 드로어 진입 1/3 — 노드 편집 | `src/pages/dialogue/NodeFormPage.spec.tsx` | FR-10-17/23, AC-10-17(오버레이 직렬화 + `UnsavedGuardContext` 미등록) — 2개 `it` |
| SIM1-D 드로어 진입 2/3 — 컨텍스트 편집 | `src/pages/dialogue/ContextFormPage.spec.tsx` | 〃 + 드로어를 열어둔 채 폼을 더 고치면 오버레이가 재직렬화됨(ui-spec §4.2-3) — 3개 `it` |
| SIM1-D 드로어 진입 3/3 — FAQ 편집 | `src/pages/dialogue/components/FaqEditModal.spec.tsx` | 〃 + 모달을 닫으면 드로어도 함께 닫힘 — 2개 `it` |

드로어 3종 시험은 `SimulatorDrawer` 자체를 가벼운 스텁으로 목킹해 "각 편집 화면이 올바른 오버레이를 만들어 드로어에 넘기는지"(배선)만 검증한다 — 드로어/시뮬레이터 내부 동작 자체는 `SimulatorPanel.spec.tsx`가 별도로 전담해 중복 시험을 피했다.

- **미자동화(수동 확인 또는 후속 과제로 명시)**: `MessageComposer`의 1,000자 초과 차단(AC-10-16), `NodeJumpPicker`/`OutputRenderer` 버튼의 키보드 Enter 실행(AC-10-15), 채널 `allowedOrigins` 빈 배열 배지(AC-11-7)·21건 상한(AC-11-8)·임베드 바로가기(AC-11-11) 전용 렌더 시험은 다음 순위. 상세 사유는 `시험항목.md` 매핑 표 참고.

#### 3.5.4 `apps/widget` — 신규 스택(Vitest + jsdom, ADR-0012)

- **2026-09-20 기준 신규 도입.** `apps/widget/package.json`에 `jsdom`을 devDependency로 추가했다(`pnpm install`로 워크스페이스 링크). 기존 `core/`(store/session/button-action/pause-schedule, DOM 무의존) 19개 단위 테스트는 `vite.config.ts`의 기본 `environment: 'node'`로 그대로 실행되고, 신규 `ui/app.spec.ts`만 파일 상단 `// @vitest-environment jsdom` 주석으로 **파일 단위 환경 오버라이드**를 적용했다(전역 설정을 `jsdom`으로 바꾸지 않아 기존 순수 로직 테스트의 실행 속도·격리를 그대로 유지).
- **jsdom의 Shadow DOM(`attachShadow`) 지원 여부를 확인**한 뒤 진행했다 — jsdom은 `attachShadow({mode:'open'})`을 정상 지원하므로, 브라우저 없이도 `mountWidgetRoot()` + `createWidgetApp()`을 실제로 마운트해 런처 클릭→패널 열림, 메시지 렌더링, 버튼 클릭 이벤트, Shadow DOM 격리(라이트 DOM에는 `#cb-widget-root` 1개만 노출되고 내부 마크업은 shadow root 안에서만 조회 가능)까지 DOM 레벨로 검증할 수 있었다. `global.fetch`를 `vi.stubGlobal`로 목킹해 공개 API 2종(`/config`, `/messages`) 왕복을 재현한다.

| 파일 | 검증 항목 |
|---|---|
| `src/ui/app.spec.ts`(신규, 7개 `it`) | AC-W-1(런처 렌더+Shadow DOM 격리), AC-W-2(런처 클릭→패널 열림+인사말), FR-W-15(퀵리플라이 버튼 렌더), AC-W-4(메시지 렌더링 순서), AC-W-9(MESSAGE 버튼 클릭→전송), AC-W-10/FR-11-25(LINK 버튼은 서버 호출 없음), AC-W-3(Esc→패널 닫힘+포커스 복귀) |
| 기존 `core/*.spec.ts`(19개, 변경 없음) | store 리듀서, session(sessionStorage+메모리 폴백), button-action(판정 위임), pause-schedule(지연 계산) |

- 실행: `pnpm --filter @chat-bot/widget test`(= `vitest run`).
- **미자동화(다음 순위)**: axe-core 스캔(AC-W-16, 위젯은 아직 jest-axe 미도입), 실 브라우저 콘솔 오류 0건 확인(AC-W-1 후반부), `renderers/{card,image,phone-call}.ts` 개별 렌더러 전용 시험, XSS 미실행(AC-W-7, innerHTML 0건은 code-reviewer가 이미 코드 리뷰로 확인), 종단간 키보드 전용 시나리오(AC-W-15) — Playwright 등 E2E 인프라 도입 시 보강.

#### 3.5.5 seed 데이터 결함 발견 및 처리 — 컨텍스트 슬롯명 정규식 위반

시험 진행 중 `apps/api/prisma/seed.ts`의 `커피주문` 컨텍스트 슬롯명이 한글("메뉴"/"사이즈"/"수량")로 되어 있어 `ContextSlotSchema.name`의 정규식(`^[A-Za-z0-9_]{1,50}$`)을 위반하는 것을 발견했다(대화설계 그룹 seed의 기존 결함, 이번 품질/채널 그룹의 결함은 아님 — 세션 재개 시 frontend-implementer가 이미 보고했던 지점과 동일 계열). PATCH 저장은 물론 오버레이 시뮬레이션도 이 슬롯 정의를 그대로 오버레이에 실으면 `400`이 난다.

**처리: seed를 직접 수정했다**(fixture 우회가 아니라 근본 수정을 선택). `slots[].name`을 `menu`/`size`/`quantity`(영문 식별자)로 바꾸고, 화면 표시는 기존 `label`("메뉴"/"사이즈"/"수량")을 그대로 유지했다. `completionMessage`의 치환자도 `{메뉴} {사이즈} {수량}` → `{menu} {size} {quantity}`로 함께 수정했다(치환 로직이 `slot.name`을 키로 쓰기 때문, `packages/dialogue-engine/src/context-session.ts`의 `renderCompletionMessage`). 다른 seed 데이터는 건드리지 않았다. 이 seed는 자동화된 통합/컴포넌트 테스트가 직접 로드하지 않으므로(각 그룹 테스트는 seed와 무관하게 자체 fixture를 생성) 기존 테스트 스위트에는 영향이 없다 — 실 서버 기동 시 수동 검증(`pnpm --filter @chat-bot/api prisma:seed`)과 위젯 수동 검증(NFR-M5)에서만 차이가 드러난다.


### 3.6 실제 구현 현황 — 보안/이력(No.12~13), 2026-09-22 시험 진행

> 대상 문서: `docs/requirements/security-audit.md` §7(AC)/§8(EX). backend-implementer/frontend-implementer 구현과 code-reviewer 1차+2차(확인) 리뷰(Critical/High 0건)까지 끝난 상태에서 시험을 진행했다. 이 그룹은 "새 기능"이 아니라 **이미 배포된 11개 컨트롤러·9개 도메인 모듈·1개 프런트 앱 전체에 인증·권한·감사를 채워 넣는 횡단 관심사 그룹**이라(§1.1), 자동화도 신규 통합 스펙 1개(`security-audit.integration.spec.ts`, 선행 세션에서 이미 구축) 보강 + `apps/web` 신규 컴포넌트/컨텍스트 시험 11개 파일이 중심이다. 세부 AC/EX ↔ 자동화 위치 매핑은 `시험항목.md`의 "보안/이력(No.12~13) 상세 시험항목" 표를 참고.

#### 3.6.1 `apps/api` — Jest(§3.1과 동일 컨벤션, 기존 파일 보강 2건)

| 계층 | 위치 | 비고 |
|---|---|---|
| 통합(HTTP 계약) | `apps/api/src/integration/security-audit.integration.spec.ts`(기존 18개 `it` + **신규 1건**) | AC-12D-5(관리자 시뮬레이션은 금지어 필터 미적용) 신규 추가 — `시험데이터.md` §11.2 |
| 단위(전수 스캔) | `apps/api/src/common/auth/public-decorator-count.spec.ts`(기존 2개 `it` + **신규 1건**) | 1차 code-reviewer 지적("알려진 핸들러만 점검, 전수 스캔 아님")을 해소 — 17개 컨트롤러 전부를 import해 라우트 핸들러를 `PATH_METADATA`로 자동 판별한 뒤 `@Public()` 총개수를 센다(AC-C-4). `시험데이터.md` §11.3 |
| 단위(순수 함수, 변경 없음) | `password-policy`/`session-expiry`/`password-hash`/`cookie`/`permission-matrix`/`last-admin`/`banned-word-filter`/`audit-range`/`audit-snapshot`/`audit-diff`/`rate-limiter`(11개 파일, 선행 세션에서 이미 구축) | 이번 시험은 전부 회귀 없이 그대로 통과 — 신규 작성 없음 |

- **AC-12D-5(코드리뷰 인계 공백)**: `SimulationService`가 애초에 `BannedWordFilterService`를 주입받지 않는 구조(FR-0-21과 동일하게 "의존성 그래프로 불가능함을 보장"하는 패턴)라는 점에 착안해, 금지어를 포함한 문장을 시뮬레이션 API로 보내 공개 대화였다면 나왔을 고정 차단 문구가 **아닌** 응답이 오는지로 "필터 미적용"을 검증했다.
- **`@Public()` 전수 스캔 보강(1차 리뷰 지적)**: 기존 테스트는 알려진 5개 핸들러(`health.check`/`publicConversation.getConfig`/`sendMessage`/`auth.login`/`logout`)만 `true`인지, 알려진 5개 핸들러(`auth.me`/`changePassword`/`chatbots.list`/`create`/`users.list`)만 `false`인지 개별 확인하는 "표본 점검"이었다. 신규 테스트는 `apps/api/src` 전 컨트롤러(17개)를 등록해 실제로 몇 개가 `@Public()`인지 **세어서** `[AuthController#login, AuthController#logout, HealthController#check, PublicConversationController#getConfig, PublicConversationController#sendMessage]` 정확히 5개와 일치하는지 확인한다. **새 컨트롤러가 추가될 때 이 파일의 import 목록도 함께 갱신해야** 스캔이 계속 전수가 된다 — `code-reviewer` 체크리스트에 추가 권고.

#### 3.6.2 `apps/web` — Vitest+Testing Library+jest-axe(§3.2와 동일 컨벤션, 신규 파일 11개, 38 tests)

> 배경: 2차 code-reviewer 리뷰가 지목한 대로, `apps/web/src/pages/auth/`·`apps/web/src/pages/settings/`·`apps/web/src/context/AuthContext.tsx`에 **자동화 테스트가 0건**이었다(구현 자체는 완료돼 있었으나 시험이 없었다). 이번 시험에서 이 공백을 전부 메웠다.

| 대상 | 파일 | 검증 항목 |
|---|---|---|
| 인증 컨텍스트(L0 게이트) | `src/context/AuthContext.spec.tsx`(6) | 부팅 시 `GET /auth/me` 1회 → status 전환(ready/unauthenticated/password-change-required), `can()`이 서버 `permissions[]`만 근거로 판정(역할 문자열 하드코딩 아님), login/logout 상태 갱신, `useAuth` 프로바이더 밖 사용 시 에러 |
| **세션만료 시 폼 데이터 보존(AC-U-4)** | `src/context/AuthContext.session-expiry.spec.tsx`(2) | `AuthProvider`+`apiClient`를 실제로 함께 동작시켜(mock 최소화) 저장 중 401 → 입력값 유지 + 재로그인 모달 → 재인증 성공 시 **원 요청 자동 재시도**, 취소 시 모달만 닫히고 입력값 유지 |
| **로그인 화면 키보드 전용(AC-U-2)** | `src/pages/auth/LoginPage.spec.tsx`(3) | autoFocus 이메일 → Tab만으로 토글버튼/비밀번호/제출버튼 순회 후 Enter 제출(마우스 클릭 0회), returnTo 복귀, 계정열거 방지 배너 |
| **403 접근거부 안내(AC-U-6)** | `src/components/security/RequirePermission.spec.tsx`(3) | 권한 없으면 안내 화면 렌더 + **주소가 시도한 경로 그대로 유지**(`useLocation` 프로브로 리다이렉트 여부를 직접 단언), 홈 링크 제공, 권한 있으면 원 콘텐츠 렌더 |
| **401 세션만료 훅(AC-U-9)** | `src/api/client.spec.ts`(6) | GET/postForm 양쪽 모두 401→재로그인→1회 재시도, 재로그인 취소 시 원 401 그대로, `/auth/*` 경로는 훅 대상 아님, 동시 다발 401은 모달 1회만 |
| **ChangePasswordForm 401 분기(2차 리뷰 Low #b)** | `src/pages/auth/ChangePasswordForm.spec.tsx`(6) | `INVALID_CREDENTIALS`→필드오류(로그아웃 안 함), `SESSION_EXPIRED`/`ACCOUNT_DISABLED`→즉시 로그아웃, 코드 없는 401→폴백 메시지, 성공 시 `refreshMe`+`onSuccess`, 비밀번호 불일치 |
| **UserFilterBar 힌트(2차 리뷰 Low #c)** | `src/pages/settings/users/UserFilterBar.spec.tsx`(4) | 상태 체크박스 0개/2개 선택 시 폴백 힌트 노출, 1개 선택 시 숨김, 체크박스 클릭 시 토글 |
| **ResourcePickerField(chatbot)(2차 리뷰 Low #a)** | `src/components/ResourcePickerField.spec.tsx`(2) | `resourceType='chatbot'`은 `chatbotId` prop 없이 전역 `chatbotsApi.list`/`findOne`을 호출(다른 4종의 챗봇 스코프 API가 아님) |
| axe 스캔(회원 관리, AC-U-11) | `src/pages/settings/UsersPage.a11y.spec.tsx`(2) | 데이터 있음/빈 상태 2케이스, 구조적 위반 0건 |
| axe 스캔(금지어 관리, AC-U-11) | `src/pages/settings/BannedWordsPage.a11y.spec.tsx`(2) | 〃 + "문장으로 시험하기" 패널 포함 |
| axe 스캔(이력 관리, AC-U-11) | `src/pages/settings/AuditLogsPage.a11y.spec.tsx`(2) | 〃 + `ResourcePickerField(chatbot)` 필터 포함 |

- **jsdom 레이블 매칭 함정**: `PasswordField`/`LoginPage`의 필수 필드는 `label + <span aria-hidden>*</span>`로 렌더되어 접근성 트리 상 레이블 텍스트에 `" *"`가 포함된다. `getByLabelText('비밀번호')`가 아니라 `getByLabelText('비밀번호 *')`로 질의해야 한다(테스트 작성 중 발견한 시행착오, 앱 코드 결함 아님).
- **모바일 카드/데스크톱 테이블 동시 렌더**: `UsersPage` 등은 반응형을 위해 데스크톱 `<table>`과 모바일 `<UserCardList>`를 **jsdom에서는 둘 다** 렌더한다(CSS 미디어쿼리로 실제 화면에서만 하나가 숨겨짐). 동일 텍스트가 두 곳에 나타나므로 `findByText` 대신 `findAllByText`를 써야 한다(다른 그룹 스펙에서도 이미 알려진 패턴, `자동시험_전략.md` §3.2와 동일한 종류의 함정).

#### 3.6.3 회귀 확인(변경 없음, 이번 시험에서 재실행만)

| 스위트 | 결과 |
|---|---|
| `pnpm --filter @chat-bot/dialogue-engine test` | 10 suites / 72 tests, 전부 Pass(AC-C-3 — 엔진 무변경 재확인) |
| `pnpm --filter @chat-bot/widget test` | 5 suites / 26 tests, 전부 Pass(AC-U-10 — 위젯 무영향 재확인) |
| `pnpm --filter @chat-bot/api typecheck` / `pnpm --filter @chat-bot/web typecheck` | 둘 다 clean(오류 0건) |


### 3.7 실제 구현 현황 — 통계/분석(No.14~15), 2026-09-22 시험 진행

> 대상 문서: `docs/requirements/stats-learning.md` §7(AC)/§8(EX), `docs/02-spec/stats-learning-설계.md`(DD-49~67, K-1~K-6). backend-implementer/frontend-implementer 구현과 code-reviewer 1차(Medium 2건 → 수정 완료) + 2차(확인, Critical/High 0건) 리뷰까지 끝난 상태에서 시험을 진행했다. 이 그룹은 `stats` 모듈 확장(읽기 전용) + `learning` 모듈 신규(쓰기)로 구성되며, `apps/web`에 통계/학습현황 화면 2종이 추가됐다. 세부 AC/EX ↔ 자동화 위치 매핑은 `시험항목.md`의 "통계/분석(No.14~15) 상세 시험항목" 표를 참고.

#### 3.7.1 `apps/api` — Jest(§3.1과 동일 컨벤션, 기존 파일 보강 + 신규 파일 2개)

| 계층 | 위치 | 비고 |
|---|---|---|
| 통합(HTTP 계약) | `apps/api/src/integration/stats-learning.integration.spec.ts`(기존 30개 `it` + **이번 시험에서 8개 순증**) | AC-14A-3/4/5/8(단위별 기본기간·KST 경계·WEEK 라벨·MONTH 합계보존), "No.14 dayBucket/hourBucket 버킷 적재·백필" describe 2건(적재 즉시성 + 백필 스크립트 실행 검증), AC-15B-11(동시 반영 경합), AC-15B-12 보강(bulk 감사 경계, NFR-S8) |
| 단위(신규) | `apps/api/src/learning/unanswered-collector.service.spec.ts`(5개 `it`) | AC-15A-11(PENDING 상한) — `ConfigService`를 목으로 직접 주입해 검증(§12.2/§12.4 근거로 HTTP 레벨 재현이 불가능함을 확인했다) |
| 단위(구조, 신규) | `apps/api/src/learning/learning-apply-invalidate-callsite.spec.ts`(1개 `it`) | DD-55 K-1 — `learning` 모듈 소스를 재귀 스캔해 `bundleService.invalidate()` 호출이 `LearningApplyService` 1곳뿐임을 구조적으로 고정(No.12~13의 `public-decorator-count.spec.ts` 전수 스캔 선례 재사용) |
| 단위(순수 함수, 변경 없음) | `stats/lib/{bucket,dashboard-aggregator,dashboard-period,kst-date,response-source,stats-period,usage-trend}.spec.ts`, `learning/lib/{collect-decision,intent-suggest,occurrence-trend,variants}.spec.ts`(11개 파일, backend-implementer 구현 단계 산출물) | 이번 시험에서 전부 회귀 없이 그대로 통과 — 신규 작성 없음 |

- **AC-14A-12(5초 타임아웃) 미자동화 판단 근거**: `stats.service.ts`의 `withTimeout()`은 `AGGREGATION_TIMEOUT_MS=5000` 상수를 코드에 고정하고 있어(환경변수화되지 않음) 결정적으로 재현하려면 실제 5초 대기 또는 내부 구조 변경이 필요하다. No.2 그룹의 동일 계열 항목(EX-2-5)과 같은 이유로 이번에도 미자동화 처리했다 — `Promise.race` 구조·오래된 캐시 미반환(DD-54)은 코드 리뷰로 확인했다.
- **AC-15A-11(PENDING 상한) HTTP 재현 불가 — 원인 규명**: `@nestjs/config`의 `ConfigModule.forRoot()`(`node_modules/@nestjs/config/dist/config.module.js`)는 `env.validation.ts`의 `validate()`를 **`forRoot()`가 호출되는 시점**(= 테스트 파일 상단의 정적 `import { AppModule }`이 평가되는 순간)에 1회 실행해 `ConfigService`의 내부 설정을 확정한다. 이는 `beforeAll`보다 먼저 일어나므로, `beforeAll` 안에서 `process.env.UNANSWERED_MAX_PENDING = '3'`를 설정해도 `ConfigService.get()`은 이미 굳어진 기본값(5000)을 반환한다(실제로 디버그 로그로 확인함 — `maxPending=5000 typeof=number`). 기본값 5,000건을 실제 HTTP 왕복으로 채우는 것도 비현실적이므로, `UnansweredCollectorService`를 NestJS DI 없이 직접 생성해 `ConfigService`를 순수 목 객체로 주입하는 단위 시험으로 대체했다. **다른 그룹이 유사하게 환경변수 override가 필요한 시험을 설계할 때 동일한 함정에 주의할 것**(`DATABASE_URL`은 예외 — Prisma가 NestJS `ConfigService`를 거치지 않고 `process.env`를 직접 읽으므로 `beforeAll`의 override가 정상 작동한다).

#### 3.7.2 `apps/web` — Vitest+Testing Library(§3.2와 동일 컨벤션, 신규 파일 2개, 16 tests)

> 배경: code-reviewer 2차 리뷰가 M-1(학습현황 하이라이트 카드 분기)·M-2(`ResolveModal` 디바운스 서버 검색)를 지적했고 frontend-implementer가 수정을 완료했으나, 두 수정사항 모두 **전용 회귀 테스트가 없는 상태**였다(기존에는 `LearningQueuePage.a11y.spec.tsx`/`StatsOverviewPage.a11y.spec.tsx`만 존재 — axe 스캔 목적의 렌더 시험이라 이 두 로직 분기를 검증하지 않는다). 이번 시험에서 이 공백을 메웠다.

| 대상 | 파일 | 검증 항목 |
|---|---|---|
| **학습현황 하이라이트 카드(M-1)** | `src/pages/learning/LearningQueuePage.spec.tsx`(6) | 목록에 이미 있는 항목은 카드 미노출(중복 방지) / 없는 항목은 액션과 함께 노출(EDITOR) / PENDING이 아니면 되돌리기만 노출 / VIEWER는 액션 숨김(FR-C-6) / 카드에서 무시 클릭 시 `refreshHighlightIfMatch`로 재조회되어 상태 갱신 / `highlightId` 없으면 카드 자체 미렌더 |
| **`ResolveModal` 디바운스 서버 검색(M-2)** | `src/pages/learning/ResolveModal.spec.tsx`(10) | 빈 입력 시 미검색 / 300ms 디바운스 타이밍(그 전엔 호출 안 함) / 초기 100건 밖 의도도 검색으로 매칭 / **경쟁 조건 방지**(느린 이전 응답이 최신 검색 결과를 덮어쓰지 않음, `latestSearchTermRef` 가드) / 검색어 삭제 시 초기 목록 복귀 / 검색 실패 시 초기 목록 폴백 / 제출 성공(기존 의도 intentId·신규 의도 intentName) / LIMIT_EXCEEDED 배너 / ALREADY_RESOLVED → `onAlreadyResolved` |

- **fake timers + userEvent 조합**: `vi.useFakeTimers({ shouldAdvanceTime: true })`와 `userEvent.setup({ advanceTimers: vi.advanceTimersByTime })`를 함께 써야 `userEvent.type()`의 내부 대기와 컴포넌트의 `setTimeout` 디바운스가 동시에 제어된다. 하나만 쓰면 입력 이벤트 자체가 걸리거나 디바운스 타이머가 진행되지 않는다.
- **`role="status"` 충돌**: 학습현황 하이라이트 카드(`div.learning-highlight-card[role="status"]`)와 성공 토스트가 같은 role을 쓴다. `screen.getByRole('status')`는 토스트 등장 이후 "여러 요소 발견"으로 실패하므로, 카드 전용 CSS 클래스를 `container.querySelector()`로 스코프하는 로컬 헬퍼를 둔다(다른 그룹의 `findAllByText` 함정과 같은 종류의 시행착오, §3.2/§3.6.2 선례와 동일 패턴).

#### 3.7.3 회귀 확인(변경 없음, 이번 시험에서 재실행만)

| 스위트 | 결과 |
|---|---|
| `pnpm --filter @chat-bot/dialogue-engine test` | 10 suites / 72 tests, 전부 Pass(엔진 무변경 재확인, J-5) |
| `pnpm --filter @chat-bot/widget test` | 5 suites / 26 tests, 전부 Pass(위젯 무영향 재확인, F-13) |
| `pnpm --filter @chat-bot/api typecheck` / `pnpm --filter @chat-bot/web typecheck` | 둘 다 clean(오류 0건) |

#### 3.7.4 사후 발견 결함(bug-triage, 2026-09-22) — `ResolveModal.spec.tsx` 모노레포 전체 동시 실행 시 간헐적 실패

> 이 그룹(No.14~15)이 §3.7.2에서 작성한 `ResolveModal.spec.tsx`(M-2 디바운스 서버 검색 회귀)가, 후속 "FAQ/의도 매칭 고도화(No.6/9/18/30 부분)" 그룹의 test-automation 단계에서 `pnpm -r test`로 모노레포 전체를 동시 실행할 때만 다음 오류로 간헐적으로 실패하는 것이 `bug-triage`에 의해 발견·기록됐다. 이 그룹(FAQ/의도 매칭 고도화)은 이 파일을 수정하지 않았으므로 신규 결함이 아니라 No.14~15 그룹의 기존 잠재 결함이다.

```
Uncaught Exception: Cannot read properties of undefined (reading 'then')
(ResolveModal.tsx:77, 디바운스 setTimeout 콜백이 테스트 종료 후 발화)
```

- **재현 조건**: `pnpm -r test`(모노레포 전체 동시 실행)에서만 간헐 재현. `vitest run src/pages/learning/ResolveModal.spec.tsx` 단독 실행(10/10) 및 `pnpm --filter @chat-bot/web test` 패키지 전체 단독 실행(170/170)은 항상 통과 — 시스템 부하가 낮을 때는 재현되지 않는다.
- **원인 분석(코드 리뷰, 코드 수정 없이 판독만 수행)**: `ResolveModal.tsx:67~87`의 디바운스 `useEffect`는 `return () => clearTimeout(timer)` cleanup을 정상적으로 반환하며, `isOpen`/`chatbotId`/`trimmedNameForSearch` 의존성 변경 시는 물론 언마운트 시에도 React가 이 cleanup을 호출해 미발화 타이머를 정상적으로 취소한다 — **프로덕션 코드 자체에는 결함이 확인되지 않았다**(실제 브라우저의 네이티브 타이머 기준으로는 안전).
  - 에러 메시지("reading 'then' of undefined")는 `intentsApi.list(...)`(모의 객체 `mockIntentsList`)가 `undefined`를 반환했다는 의미로, 실제 서버 호출(항상 Promise 반환)에서는 발생할 수 없는 형태다.
  - 유력한 원인은 이 스펙의 `vi.useFakeTimers({ shouldAdvanceTime: true })` + `afterEach(() => vi.useRealTimers())` 조합이다. `shouldAdvanceTime: true`는 내부적으로 실시간에 연동돼 가짜 시계를 자동 전진시키는 보조 타이머를 두는데, 모노레포 전체 동시 실행으로 CPU 경합이 심해지면 어느 한 테스트 케이스의 디바운스 콜백이 다음 케이스의 `vi.useRealTimers()`/`mockIntentsList.mockReset()` 실행 시점과 경합해, **초기화된(구현이 없는) mock**을 향해 뒤늦게 발화할 수 있는 것으로 추정된다(§3.7.2에 이미 기록한 "fake timers + userEvent 조합" 시행착오와 같은 계열의 타이밍 함정).
- **등급 판정(`오류검출_프로세스.md` 기준)**: **Low** — 실제 사용자에게 노출되는 프로덕션 코드 결함이 아니라 여러 패키지를 동시 실행할 때만 나타나는 **테스트 인프라 안정성 문제**(신뢰도 저하)로 판단한다. Critical/High(서비스 장애·핵심기능 불가)에 해당하지 않으므로 `자동배포.md`의 배포 게이트를 잠글 필요는 없다(현재 3단계 CI 자체도 미착수 상태).
- **라우팅**: 원인 파일이 No.14~15 그룹 소관(`ResolveModal.spec.tsx`, 커밋 `bd4ae05`)이므로 **frontend-implementer**(No.14~15 그룹 재작업 담당)에게 backlog로 전달한다. 조치 후보: ① 각 `it`에서 `unmount()`를 명시적으로 호출해 실행 순서와 무관하게 타이머 취소를 보장, ② `shouldAdvanceTime: true` 제거하고 `vi.advanceTimersByTimeAsync`만으로 결정적으로 시간 진행, ③ `afterEach`에 `vi.clearAllTimers()`를 `vi.useRealTimers()`보다 먼저 호출해 잔여 타이머를 명시적으로 제거. **코드/스펙 수정은 이번 세션에서 하지 않았다** — 사용자 승인 후 별도 세션에서 진행.
- 교차참조: `docs/04-test/오류검출_프로세스.md` §6 "결함 이력"에도 동일 건을 기록했다.


### 3.8 실제 구현 현황 — FAQ/의도 매칭 고도화(No.6/9/18/30 부분), 2026-09-22 시험 진행

> 대상 문서: `docs/requirements/nlu-rag-answering.md` §7(AC)/§8(EX), `docs/02-spec/nlu-rag-answening-설계.md`, `docs/02-spec/decisions/ADR-0020~0024`, `docs/03-design/nlu-rag-answering-ui-spec.md`(§10 "알려진 제한사항" 포함). 1단계(NLU 의미 유사도 매칭, `apps/ml-worker` 신설 포함)·2단계(외부 RAG 서버 폴백)의 backend-implementer/ml-engineer/frontend-implementer 구현과 code-reviewer 2차 검증(공백 3건 지적)까지 끝난 상태에서 시험을 진행했다. 세부 AC/EX ↔ 자동화 위치 매핑은 `시험항목.md`의 "FAQ/의도 매칭 고도화(No.6/9/18/30 부분) 상세 시험항목" 표를 참고.

#### 3.8.1 `apps/api` — Jest(§3.1과 동일 컨벤션, 신규 파일 3개)

| 계층 | 위치 | 비고 |
|---|---|---|
| 단위(환경변수 검증) | `apps/api/src/config/env.validation.spec.ts`(6개 `it`, 신규) | AC-N2-14(`RAG_TIMEOUT_MS` 하한 120,000ms/상한 300,000ms 클램프 + 경고 로그), AC-N4-1(RAG/임베딩 미설정 기동). **`env.validation.ts`는 이번까지 전용 시험 파일 자체가 없었다** — `validate()`가 순수 함수라 NestJS 부트스트랩 없이 직접 호출해 검증한다 |
| 단위(오케스트레이션) | `apps/api/src/rag/rag-answer.service.spec.ts`(12개 `it`, 신규) | `RagAnswerService.run()`(§9.2~§9.9) 전체 배선 — 성공/무근거/LLM오류 접두어/PII마스킹(J-9)/재시도 정책(429·503×2종·408/500/400·네트워크오류)/스키마불일치/vLLM미준비/미설정, 전부 `RagHttpClient`/`RagGateService`/`RagCallLogService`/`PendingAnswerStore`/`BannedWordFilterService`를 목(mock)으로 주입해 실제 오케스트레이션 코드를 통과시킨다. 개별 순수 함수(`judge-rag-response`/`sanitize-sources`/`should-run-rag`)는 이미 있었으나 이들을 실제로 연결한 배선은 미검증이었다 |
| 단위(HTTP 클라이언트) | `apps/api/src/rag/rag-http.client.spec.ts`(9개 `it`, 신규) | `RagHttpClient`(외부 RAG 서버로 나가는 유일한 출구) 자체의 요청 조립 검증 — AC-N2-2(`provider:'pdf'` 상수·`Content-Type` 헤더), FR-N2-8(임계값 미지정 시 필드 자체 생략), FR-N2-10(200자 절단), allowlist 3경로(`QUERY`/`STATUS`/`DOCUMENT_METADATA`)만 호출, 네트워크 예외/HTTP 오류/JSON 파싱 실패의 `RagSendResult` 판정. `global.fetch`를 `http-embedding.provider.spec.ts`와 동일한 패턴(jest mock)으로 모킹한다 |
| 단위(순수 함수, 변경 없음) | `rag/lib/{judge-rag-response,sanitize-sources,should-run-rag,parse-document-metadata,rag-allowlist}.spec.ts`, `rag/{rag-gate.service,pending-answer.store}.spec.ts`, `embedding/providers/{http-embedding,mock-embedding}.provider.spec.ts`(backend-implementer/ml-engineer 구현 단계 산출물) | 이번 시험에서 전부 회귀 없이 그대로 통과 — 재작성 없음. `rag-allowlist.spec.ts`(AC-N2-1/NFR-S11, 파괴적 엔드포인트 문자열 0건 정적 검사)는 이번 시험에서 **재확인만** 했다 |
| 통합(HTTP 계약) | `apps/api/src/integration/nlu-rag-answering.integration.spec.ts`(backend-implementer 구현 단계 산출물, 변경 없음) | `EMBEDDING_BASE_URL`/`RAG_BASE_URL`을 의도적으로 비워 둔 상태(AC-N4-1)에서 신규 API 7개(`answer-settings` CRUD/preview/test, `embeddings` status/reindex, 공개 폴링)를 계약 레벨로 검증. **RAG 활성 상태의 종단 통합은 이 스위트 밖**이다(§3.8.3 참고) |

**code-reviewer 2차 검증 지적사항 3건 처리** — 상세 근거·픽스처는 `시험데이터.md` §13.1 참고:

1. `AnswerSettingsPreviewPanel`의 `dirty` 배지 노출 미검증 → `apps/web/src/pages/chatbot-detail/answer-settings/AnswerSettingsPreviewPanel.spec.tsx`(2 tests, §3.8.2).
2. `allowStackedLayout`(되묻기 세로 스택 판정) 전용 시험 없음 → `apps/widget/src/ui/renderers/button.spec.ts`(7 tests) + `apps/web/src/components/OutputRenderer.spec.tsx`(5 tests).
3. ui-spec §10 "알려진 제한사항" 회귀 고정 → 위 두 파일에 "알려진 제한사항 회귀 고정" describe 블록 추가(문서화된 오탐이 실제로 재현됨을 확인 — 버그로 취급하지 않음).

#### 3.8.2 `apps/web` — Vitest+Testing Library(§3.2와 동일 컨벤션, 신규 파일 2개)

| 대상 | 파일 | 검증 항목 |
|---|---|---|
| 임계값 미리보기 미저장 경고 배지 | `src/pages/chatbot-detail/answer-settings/AnswerSettingsPreviewPanel.spec.tsx`(2) | `formDiffersFromSaved=true`→`SeverityBadge(WARNING)` 노출, `=false`(기본값)→미노출(UIUX §1 색상 단독 전달 금지 — 아이콘+텍스트 병기) |
| 되묻기 세로 스택 판정(`allowStackedLayout`) | `src/components/OutputRenderer.spec.tsx`(5) | 전원 `MESSAGE`+라벨 10자 초과 → 스택, 10자 이하 → 비스택(경계값), `NODE` 혼재 시 비스택, `CARD` 버튼은 구조적으로 항상 비스택. §10 "알려진 제한사항"(일반 퀵메뉴 오탐) 회귀 고정 포함 |

#### 3.8.3 `apps/widget` — Vitest+jsdom(§3.5.4와 동일 컨벤션, 신규 파일 2개)

| 파일 | 검증 항목 |
|---|---|
| `src/ui/renderers/button.spec.ts`(7개 `it`, 신규) | `renderButtonGroup`/`renderCard`의 `allowStackedLayout` 판정 — `apps/web`의 `OutputRenderer.spec.tsx`와 동일 휴리스틱을 마크업만 다르게 검증(양쪽 앱이 판정 로직을 각자 구현하고 있어 회귀 시험도 각자 둔다). `renderers/{button,card,index}.ts`는 이번까지 전용 단위시험이 없었다 |
| `src/ui/app.pending.spec.ts`(4개 `it`, 신규) | **공개 위젯 통합 시나리오**(질문 전송 → `pendingAnswer` 즉시 응답 → 폴링 → 완료) — 인터림 말풍선+진행 인디케이터(입력 잠금 없음, FR-N2-38/39), `READY`(출처 포함, NFR-A3 링크 아님)/`FAILED`(RAG 실패 원문 미노출, FR-N2-19)/`EXPIRED`(정리 문구, S-15) 3종 종결, 도중 새 질문 시 이전 폴링 폐기(EX-N2-11) |

**jsdom `CSS.escape` 미구현 대응(테스트 인프라 보강, 앱 코드 결함 아님)**: `app.pending.spec.ts` 작성 중 `renderers/pending-indicator.ts`(`removePendingIndicator`가 `CSS.escape()`로 `data-pending-id` 선택자를 조립)가 jsdom 25.x에서 `TypeError: Cannot read properties of undefined (reading 'escape')`로 실패하는 것을 발견했다. jsdom은 `CSS.escape`를 구현하지 않는다(jsdom/jsdom#1076, 수년째 미해결)는 알려진 한계이며, 실제 배포 대상 브라우저(Chrome/Firefox/Safari/Edge 최신)는 전부 지원하므로 **프로덕션 동작에는 영향이 없다**. `apps/widget/src/test/css-escape-polyfill.ts`(신규, CSSOM 표준 알고리즘의 축약 구현, 신규 런타임 의존성 0건)를 `vite.config.ts`의 `test.setupFiles`에 등록해 해소했다 — 프로덕션 번들에는 포함되지 않는다(`build` 스크립트는 `fullscreen.ts`/`widget.js` 로더만 엔트리로 사용).

#### 3.8.4 공개 위젯 통합 시나리오(질문 전송 → PENDING → 폴링 → 완료) — 3계층 조합 검증 판단

과제에서 요구한 "공개 위젯 통합 시나리오"는 실제 외부 RAG 서버·실제 공개 대화 API HTTP 왕복까지 포함한 **단일 종단 통합 테스트로는 구현하지 않았다**. 이유:

- `nlu-rag-answering.integration.spec.ts`는 `RAG_BASE_URL` 미설정을 전제로 부팅된다 — 이는 우연이 아니라 그 스위트 자체의 존재 이유(AC-N4-1: "신규 환경변수를 하나도 설정하지 않아도 정상 기동")이므로, 같은 스위트 안에서 RAG를 활성화하면 오히려 AC-N4-1의 회귀 방지력이 약해진다.
- RAG 활성 상태의 **별도** Nest 앱을 추가로 부팅(`prisma db push` + `app.listen()`)하는 비용은 크고, `global.fetch`를 이중으로 모킹(공개 API 왕복 + 외부 RAG 왕복)해야 해 테스트 자체의 신뢰도도 오히려 떨어진다.

대신 **3개 레이어를 조합**해 같은 경로를 각 경계에서 정밀하게 검증했다(§3.8.1/§3.8.3):

1. 위젯 DOM 레벨(`app.pending.spec.ts`) — 위젯이 실제로 소비하는 계약(`pendingAnswer` → 폴링 → `READY`/`FAILED`/`EXPIRED`).
2. 백엔드 오케스트레이션 레벨(`rag-answer.service.spec.ts`) — `PendingAnswerStore.complete()`/`ConversationLogPort.record()`가 올바른 인자로 호출되는지.
3. 외부 HTTP 클라이언트 레벨(`rag-http.client.spec.ts`) — 실제로 나가는 요청의 모양.

3개 프로세스를 모두 살려 실제 네트워크 스택까지 왕복하는 단일 E2E 테스트는 여전히 없다 — Playwright 등 E2E 인프라 도입 시 1순위로 추가할 것(§4.7 참고). 외부 RAG 서버를 실제로 호출하는 테스트는 어떤 레이어에서도 CI에 넣지 않는다(전부 모킹 — `nlu-rag-answering.md` §11 인계 메모 "test-automation ⑩" 원칙 준수).

#### 3.8.5 회귀 확인(변경 없음, 이번 시험에서 재실행만)

| 스위트 | 결과 |
|---|---|
| `pnpm --filter @chat-bot/dialogue-engine test` | 11 suites / 86 tests, 전부 Pass(`semantic-match.spec.ts` 포함, 엔진 순수성 회귀 없음) |
| `pnpm --filter @chat-bot/pii-mask test` | 1 suite / 8 tests, 전부 Pass(AC-N4-7 — 마스킹 함수 1벌 재확인) |
| `pnpm --filter @chat-bot/api test`(Jest) | **54 suites / 459 tests**, 전부 Pass(신규 3파일 27 tests 포함 — env.validation 6 + rag-answer.service 12 + rag-http.client 9) |
| `pnpm --filter @chat-bot/web test`(Vitest) | **38 files / 170 tests**, 전부 Pass(신규 2파일 7 tests 포함) |
| `pnpm --filter @chat-bot/widget test`(Vitest) | **8 files / 45 tests**, 전부 Pass(신규 2파일 11 tests 포함) |
| `apps/ml-worker`(pytest, `node ./scripts/run.mjs -m pytest -q`) | 9 tests, 전부 Pass(변경 없음, 계약 검증 mock 기반) |
| `pnpm --filter @chat-bot/api typecheck` / `@chat-bot/web typecheck` / `@chat-bot/widget typecheck` | 전부 clean(오류 0건) |
| `npx eslint <신규 파일 7개>` | 오류 0건 |



### 3.9 실제 구현 현황 — 학습 고도화(No.16/23 — 예문 증강 + 요소분해/경량 분류기), 2026-09-22 시험 진행

> 대상 문서: `docs/requirements/learning-augmentation.md` §7(AC)/§8(EX), `docs/02-spec/learning-augmentation-설계.md`, `docs/02-spec/decisions/ADR-0025~0028`. 이 그룹은 구현이 이미 완료된 상태(`apps/api/src/augmentation/**`·`classifier/**`·`training-jobs/**`, `apps/web`의 증강 패널·분류기 상태 패널)에서 시험을 진행했다 — 신규 기능 개발이 아니라 **커버리지 공백 확인/보강**이 목적이다(오케스트레이터가 지정한 8개 항목: J-9 무교체 회귀·분류기 대화 무영향·증강 검증 5종 경계값·요소분해 오프셋 한계·VIEWER 게이팅 양축·기동 시 고아 Job 정리·공개 API 성능 무영향). 세부 AC/EX ↔ 자동화 위치 매핑은 `시험항목.md`의 "학습 고도화(No.16/23)" 절 표를 참고.

#### 3.9.1 `apps/api` — Jest(§3.1과 동일 컨벤션, 신규 파일 6개)

| 계층 | 위치 | 비고 |
|---|---|---|
| 단위(J-9 회귀, 2번째 allowlist) | `apps/api/src/learning/decomposed-resolve.service.spec.ts`(10 tests, 신규) | `DecomposedResolveService`는 구현 시점에 전용 스펙이 없었다(오류검출 갭) — `AugmentationAcceptService.spec.ts`와 같은 형식으로 신설. `appliedImmediately`가 `applyLearning()` 반환값을 그대로 전달할 뿐임을 정상값+가상 반전값(QUEUED) 양쪽으로 고정, 스팬 검증 400, CAS 동시성 409, 부분 실패 시 `applyLearning()` 1회 유지 |
| 단위(고아 Job 정리) | `apps/api/src/training-jobs/training-job.service.spec.ts`(8 tests, 신규) | `TrainingJobService`도 전용 스펙이 없었다(오류검출 갭). `onModuleInit()`의 `QUEUED`/`RUNNING` → `FAILED`(`SERVER_RESTART`) 일괄 전이, 종료 상태 미포함 고정, CRUD 계약 |
| 단위(증강 검증 경계값) | `apps/api/src/augmentation/lib/validate-candidates.boundary.spec.ts`(16 tests, 신규) | 5종 검증(의미보존/신규성/타의도충돌/금지어/형식) 각각의 임계값 안팎. `cosineSimilarity` 반환값 직접 주입(①②③)으로 부동소수점 정밀도 문제 회피(§14.1 참고). **금지어 필터 Medium 결함 1건 발견**(§3.9.3) |
| 단위(요소분해 오프셋 한계) | `apps/api/src/learning/lib/decompose.offset-limits.spec.ts`(5 tests, 신규) | 기존 `decompose.spec.ts`(재작성 없음)를 보완 — gazetteer 부분 겹침 시 어절 전체가 통째로 스킵되는 문서화된 한계를 명시 재현, 동일 길이 tie-break, 형태소 이상값 무시 |
| 정적 검사(대화 성능 무영향) | `apps/api/src/conversation/public-conversation-classifier-isolation.spec.ts`(20 tests, 신규) | `asset-write-sealing.spec.ts`와 동일한 스캔 패턴으로 `apps/api/src/conversation/**` + `ConversationModule` import 목록 + `UnansweredCollectorService`에 classifier/augmentation 심볼 0건임을 검증(NFR-LP1/AC-L4-5의 정적 증거) |
| 통합(HTTP 계약 + 행동적 증거) | `apps/api/src/integration/learning-augmentation.integration.spec.ts`(4 tests, 신규) | ① 분류기 학습 전/후 공개 대화 응답이 바이트 단위로 동일(J-6/AC-L4-5의 행동적 증거 — mock 임베딩 서버 자체 구축, §14.4 참고) ② VIEWER 403 게이팅 백엔드 축(증강 생성/승인/거절/분류기 재학습 4종 + DB 무변경) ③ No.15 기존 `resolve` 무회귀 + `resolve-decomposed` 통합 반영 ④ 별도 describe로 앱 재부팅을 실제 재현해 고아 Job 정리(EX-L2-7)를 통합 레벨로도 확인 |
| 단위(기존, 변경 없음) | `augmentation-accept.service.spec.ts`/`validate-candidates.spec.ts`/`rule-variants.spec.ts`/`augmentation-provider.factory.spec.ts`/`asset-write-sealing.spec.ts`/`classifier/lib/{logistic-regression,stale-judge}.spec.ts`/`learning/lib/decompose.spec.ts` 등(backend-implementer 구현 단계 산출물) | 이번 시험에서 전부 회귀 없이 그대로 통과 — 재작성 없음 |

#### 3.9.2 `apps/web` — Vitest+Testing Library(§3.2와 동일 컨벤션, 신규 파일 2개)

| 대상 | 파일 | 검증 항목 |
|---|---|---|
| 증강 패널 VIEWER 게이팅(FR-L3-9/AC-L3-4) | `src/pages/dialogue/components/AugmentationPanel.viewer.spec.tsx`(4) | `useAuth().can('dialogue:write')`가 false면 생성 버튼·체크박스·승인/거절 버튼 미렌더, true면 렌더 |
| 분류기 상태 패널 VIEWER 게이팅 | `src/pages/learning/ClassifierStatusPanel.viewer.spec.tsx`(2) | `canWrite` prop이 false면 재학습 버튼 미렌더(이 컴포넌트는 컨텍스트 훅이 아니라 prop으로 게이팅한다는 차이가 있음) |

#### 3.9.3 발견된 결함 — 증강 금지어 필터의 공백 전용 항목 오탐(Medium)

`apps/api/src/augmentation/lib/validate-candidates.ts`의 `containsBannedWord()`가 `bannedWords.some((w) => w && normalized.includes(normalizeText(w)))`로 구현되어 있다. `BannedWord` 목록에 공백만으로 된 항목(`"   "`)이 있으면 `w`는 truthy라 falsy 가드를 통과하고, `normalizeText()`가 `trim()`으로 이를 빈 문자열로 바꿔 `normalized.includes("")`가 항상 `true`가 된다 — **그 챗봇의 모든 증강 후보가 예외 없이 `BANNED_WORD`로 제외**되어 No.16이 사실상 완전히 무력화되지만 화면은 "생성 N건 중 0건 통과"만 보여줘 관리자가 원인을 진단할 수 없다.

- 등급: **Medium**(크래시·데이터 손상 없음, 기능은 동작하나 특정 데이터 조합에서 완전 오동작 — `오류검출_프로세스.md` §1 기준)
- 회귀 센서: `validate-candidates.boundary.spec.ts` › "④ 금지어" › "⚠ 오류검출 — 공백만으로 된("   ") 금지어 항목은 …"(현재 결함 동작을 고정 — 수정 후 기댓값 반전 필요)
- 코드 수정은 이번 시험 범위에서 수행하지 않음(`오류검출_프로세스.md` §3 절차에 따라 보고만 함) — `backend-implementer`(No.16/23 그룹) 인계

#### 3.9.4 회귀 확인(변경 없음, 이번 시험에서 재실행만)

| 스위트 | 결과 |
|---|---|
| `pnpm --filter @chat-bot/api test`(Jest) | **73 suites / 689 tests**, 전부 Pass(신규 6파일 63 tests 포함 — 시험 전 67 suites/624 tests) |
| `pnpm --filter @chat-bot/web test`(Vitest) | **40 files / 176 tests**, 전부 Pass(신규 2파일 6 tests 포함 — 시험 전 38 files/170 tests) |
| `apps/ml-worker`(pytest) | 변경 없음(이 그룹은 ml-worker 파일을 건드리지 않음 — 미재실행) |

#### 3.9.5 이번 시험에서 종단 통합으로 만들지 않은 것 — 분류기 학습의 실제 정확도 검증

`learning-augmentation.integration.spec.ts`의 mock 임베딩 서버는 `POST /embed`에 항상 0벡터를 반환한다 — **학습이 실제로 의미 있는 정확도를 내는지는 검증하지 않으며, 오직 "학습이 성립하고 대화에 영향을 주지 않는다"만 검증한다.** 분류기의 실제 예측 품질(FR-L2-32의 골든셋 비교)은 `apps/api/src/classifier/eval/compare-classifier-vs-knn.ts`(ml-engineer 오프라인 산출물, `classifier-vs-knn-comparison.md`)가 별도로 담당하며 CI 스위트 밖이다 — FAQ/RAG 그룹의 골든셋 스윕(AC-N1-12)과 같은 성격의 "산출물(비-CI)" 분류다.
## 4. 우선 자동화 대상

### 4.1 1차(이 그룹 완료 시점 기준 재확인)

1. 대화 품질 회귀 시험(No.19 연계) — 매뉴얼 검증 대비 효과가 가장 큼 (미착수, No.19 구현 후)
2. 관리 콘솔 CRUD 핵심 플로우 E2E(의도/키워드/대화그래프 등록~저장~노출) — **No.1~4·No.5~9 모두 컴포넌트 레벨로 부분 달성(§3.2, §3.4)**, No.10 시뮬레이터 착수 시 확장
3. 채널 Webhook 수신·응답 계약(contract) 시험 (미착수, No.11 구현 후)

### 4.2 다음 자동화 대상(No.1~4 완료 후 식별된 과제)

1. **Playwright(또는 동급) E2E 도입** — AC-5-3(그룹트리→리스트→설정폼→저장 종단간 키보드 플로우) 및 AC-5-4(실 브라우저 axe 대비 스캔)를 완전히 자동화하려면 필요. `apps/widget` 착수 전까지는 `apps/web` 관리 콘솔 한정으로 범위를 좁혀 도입 검토.
2. `CopyButton`(클립보드 성공/폴백, AC-4-8/EX-4-2), `SkinPreviewPanel`(실시간 반영 AC-4-1, XSS 이스케이프 AC-4-11) 전용 컴포넌트 시험 추가.
3. CI 워크플로 구성(§2 "현재 상태" 참고) — 플랫폼/자격증명 확정 후 `docs/05-ops/자동배포.md` §1 절차를 따른다.

### 4.3 다음 자동화 대상(대화 설계 No.5~9 완료 후 식별된 과제, 2026-09-20)

1. `NodeFormPage` 종단 플로우(생성→저장→AC-5-1/5-2/5-3) — `ResourcePickerField`가 의도/키워드/컨텍스트/노드 검색 API를 호출하므로 `resourcePickerApi` mock이 별도로 필요하다. 이번 시험은 `DialogOutputEditor`를 페이지에서 분리해 단위 시험했고, 페이지 전체 종단 시험은 다음 순위.
2. `ContextFormPage`(슬롯 편집 + 대화형 미리보기, FR-8-7), `HomonymEditModal`(의미 반복 블록 추가/삭제/순서변경, FR-7-10), `FaqEditModal`(유사 후보 추천 배너, AC-9-3) 전용 컴포넌트 시험 — 아직 없음.
3. 대량 업로드 진행 중 브라우저 이탈(EX-I-8), 5,000행 실측 성능(NFR-P5)은 자동화 난이도가 높아 코드 리뷰/수동 확인으로 대체 중 — Playwright 또는 부하시험 도구(k6) 도입 시 재검토.
4. No.10(응답 테스트/시뮬레이션) 구현 시 `resolveResponse`의 `trace` 필드를 활용한 "왜 이 응답이 나왔는지" 시뮬레이터 UI 컴포넌트 시험이 신규로 필요하다.

### 4.4 다음 자동화 대상(품질/채널 No.10~11 완료 후 식별된 과제, 2026-09-20)

1. `apps/widget`에 axe-core(jest-axe 또는 동급) 도입 — AC-W-16(대비 4.5:1 스캔). `apps/web`은 이미 도입되어 있으므로(§3.3) 동일 패턴을 재사용할 수 있다.
2. `HomonymEditModal`(의미 반복 블록 UI, FR-7-10)은 여전히 전용 컴포넌트 시험이 없다(§4.3 항목 2에서 이월). `NodeFormPage`/`ContextFormPage`/`FaqEditModal`은 이번에 **SIM1-D 드로어 진입 배선만** 시험했고, 저장 버튼 클릭→API 호출까지의 전체 CRUD 플로우는 여전히 미작성이다.
3. Playwright(또는 동급) E2E 도입 시 우선순위에 추가할 항목: AC-W-15(위젯 키보드 전 과정), AC-C-2(품질/채널 관리자 화면 키보드 전 과정), AC-C-3(6탭 전체 라우팅 회귀), AC-11-6(채널 upsert 동시 생성 경합) — 전부 실 브라우저/실 동시성 환경이 필요해 jsdom·단일 프로세스 통합 테스트로는 재현이 어렵다.
4. `PublicChatbotConfigSchema`/`PublicMessageResponseSchema`를 `.strict()`로 전환할지 `system-architect`와 협의 — 전환되면 AC-C-5(여분 필드 유출)를 런타임 계약 테스트로 완전히 자동화할 수 있다(현재는 TS 컴파일 체크에 의존, §3.5.1 참고).
5. 오버레이 종류별 21건 초과(AC-10B-5), `allowedOrigins` 21건 초과(AC-11-8), 비교 21문장 초과(AC-10B-11) — 전부 zod `.max(20)` 스키마로 이미 강제되고 있으나, 프레임워크 신뢰가 아니라 HTTP 계약 레벨로 명시 검증하는 전용 통합 테스트를 추가하면 좋다(이번 시험에서는 code-reviewer 지적사항이었던 **크기(바이트)** 상한만 우선 처리했다).
6. 대시보드-공개API 연계(AC-P-9), 캐시 즉시 무효화(AC-P-14/EX-10-6), 로그 적재 실패 시 가용성(AC-P-10) — 전부 여러 모듈에 걸친 시나리오라 통합 테스트 설계 비용이 크다. `자동시험_전략.md` §4.2의 CI 워크플로 도입과 함께 재검토.

### 4.5 다음 자동화 대상(보안/이력 No.12~13 완료 후 식별된 과제, 2026-09-22)

1. **AC-13-7(9개 도메인 모듈 전수 소급 증명)** — 이번 시험은 `ChatbotGroup` 1개 모듈만 대표로 통합 테스트에 남겼다. 나머지 8개 모듈(`chatbots`/`intents`/`keywords`/`homonyms`/`contexts`/`dialog-nodes`/`faqs`/`channels`)의 CREATE/UPDATE/DELETE 소급 기록은 code-reviewer가 1차 리뷰에서 각 서비스의 `AuditLogService.record()` 호출 지점을 코드 리뷰로 전수 확인했으나 자동시험은 없다 — 이 그룹에서 **가장 큰 공백**이므로 최우선 순위로 올린다. 패턴은 `security-audit.integration.spec.ts`의 기존 "AC-13-*" 케이스를 8회 반복하면 되므로 구현 비용은 낮다.
2. **`@Public()` 전수 스캔 유지보수 규칙** — §3.6.1의 신규 컨트롤러 목록은 수동으로 import를 추가하는 방식이라, 신규 컨트롤러 파일이 생겨도 이 스펙에 추가하는 것을 잊으면 그 컨트롤러는 스캔에서 조용히 빠진다. `code-reviewer` 체크리스트에 "신규 `*.controller.ts` 추가 시 `public-decorator-count.spec.ts`의 import 목록 갱신"을 명시하거나, 후속으로 `ts-morph`/glob 기반 완전 자동 탐색으로 전환하는 것을 검토한다(현재는 신규 런타임 의존성 0건 원칙을 지키기 위해 수동 목록을 택함).
3. **회원 관리 화면의 실제 CRUD 종단 시험 부재** — `UsersPage.a11y.spec.tsx`/`BannedWordsPage.a11y.spec.tsx`/`AuditLogsPage.a11y.spec.tsx`는 axe 스캔 목적의 렌더 시험이라 `CreateUserModal`/`ChangeRoleModal`/`BannedWordFormModal`의 실제 제출→API 호출→토스트까지의 종단 플로우는 다루지 않는다. 대화설계 그룹의 `IntentsKeywordsPage.spec.tsx` 같은 "CRUD 플로우" 전용 스펙을 다음 순위로 추가한다.
4. **AC-U-5/AC-U-7(권한별 버튼/메뉴 숨김)** — `FR-U-4`(권한 없으면 렌더하지 않음)는 각 페이지의 `canWrite && (...)` 패턴을 코드 리뷰로 확인했으나, VIEWER로 로그인했을 때 실제로 버튼이 DOM에 없는지 검증하는 전용 컴포넌트 시험은 없다. `UsersPage`/`BannedWordsPage`에 `can: () => false`로 렌더해 저장/추가/케밥 메뉴가 `queryBy*`로 안 잡히는지 확인하는 케이스를 추가하면 좋다.
5. **AC-12A-9/AC-12C-9/10(세션 무효화 계열)** — 비밀번호 변경·계정 비활성화·비밀번호 초기화가 **다른 세션**을 실제로 끊는지는 코드 리뷰로만 확인했다. 통합 테스트에서 세션 A로 로그인 상태를 만든 뒤 세션 B에서 비밀번호를 바꾸고 세션 A가 401이 되는지 확인하는 케이스가 필요하다(AC-12B-8의 "역할 강등 즉시 반영" 테스트와 같은 패턴을 재사용할 수 있다).
6. Playwright(또는 동급) E2E 도입 시 우선순위에 추가할 항목: 로그인→대시보드→시스템설정 3종 화면의 종단간 키보드 전용 시나리오(§4.2~4.4에서 이월된 동일 제약과 합류), 세션 만료 모달의 실 브라우저 포커스 트랩 확인.
7. 이력 대용량 성능(AC-13-11/16, 10만 건 P95) — 대화설계/품질채널 그룹의 동일 계열 미자동화 항목(AC-6-10, NFR-P5 등)과 같은 이유로 미착수. k6 등 부하시험 도구 도입 시 함께 재검토(§4.2 항목 3과 합류).

### 4.6 다음 자동화 대상(통계/분석 No.14~15 완료 후 식별된 과제, 2026-09-22)

1. **`/stats/questions`·`/stats/distribution` 전용 HTTP 계약 케이스 부재** — AC-14B-1/3/6/7/8은 대부분 No.2 그룹의 `dashboard-aggregator.spec.ts` 단위 시험(순수 함수 재사용, FR-14-2)으로 간접 커버되지만, `/stats/summary`의 정확한 수치 조합(turnCount:100/answered:90/blocked:4 등)이나 `/stats/questions`의 금지어 제외·PII 마스킹을 **HTTP 레벨로 명시 단언**하는 전용 케이스는 없다. `stats-learning.integration.spec.ts`에 이 그룹만 대표 1~2케이스씩 추가하면 좋다.
2. **`BulkResolveModal.tsx`의 100건 의도 제한(코드리뷰 Medium 백로그)** — `ResolveModal.tsx`는 M-2로 디바운스 서버 검색을 얻었지만 `BulkResolveModal.tsx`는 상위에서 전달받은 초기 100건 `intentOptions`로만 매칭한다. 101번째 이후 의도를 대상으로 일괄 반영하면 **이미 있는 의도를 "새 의도"로 오판해 중복 생성**할 수 있다(`시험항목.md`의 "알려진 미해결 이슈" 참고). 수정 후 `BulkResolveModal.spec.tsx`(현재 전용 스펙 없음)를 신설해 회귀를 고정할 것.
3. **`ChartFrame`(표 보기 토글, 요약 문장 `aria-label`) 전용 컴포넌트 시험 부재** — AC-UI-2/3은 `StatsOverviewPage.a11y.spec.tsx`의 axe 스캔으로 구조적 접근성 위반만 확인됐을 뿐, 토글 클릭 시 실제로 `<table>`이 렌더되는지·`role="img" aria-label`이 요약 문장과 일치하는지는 전용 케이스가 없다.
4. **미응답 큐 목록 정렬(AC-15B-1)·버튼 턴 제외(AC-15A-4)·재발생 자동유지(AC-15A-10)의 HTTP 종단 케이스** — 전부 코드 리뷰로 로직만 확인했다. AC-15A-4는 `quality-channel` 그룹의 버튼 클릭 시나리오와 조합해야 하고, AC-15A-10은 같은 질문을 반영 → 재유입시키는 2단계 시나리오라 `stats-learning.integration.spec.ts`에 추가 케이스 2~3건으로 해소 가능하다.
5. **K-2(요청당 정확히 1회 무효화 호출) 정량 검증** — 현재는 `DialogueBundleService.invalidate()`가 예외 없이 실행되는 것으로 간접 확인했다. `jest.spyOn(bundleService, 'invalidate')`로 실제 호출 횟수를 세는 단위 시험(`LearningApplyService`/`UnansweredQuestionsService` 대상)을 추가하면 K-2를 정량적으로 고정할 수 있다.
6. **`GET /stats/export`(P2, DD-66)** — 이번 Phase 구현 범위 밖이라 대응 AC가 없다. 후속 구현 시 CSV 마스킹 우회 경로 검토(NFR-S6)와 함께 신규 통합 케이스가 필요하다.
7. Playwright(또는 동급) E2E 도입 시 우선순위에 추가할 항목: 기간변경→필터→선택→일괄반영 종단간 키보드 시나리오(AC-UI-4, §4.2~4.5에서 이월된 동일 제약과 합류), 폴백 분포 조각 클릭→학습현황 이동(AC-UI-8)의 실 라우팅 확인.
8. AC-14A-12(5초 타임아웃)의 결정적 재현 — `AGGREGATION_TIMEOUT_MS`를 환경변수화(`STATS_AGGREGATION_TIMEOUT_MS` 등)하면 테스트에서 낮은 값으로 override해 짧은 대기로 타임아웃을 강제할 수 있다. 단 이는 `system-architect`가 판단할 설계 변경이며 이번 시험 범위(테스트 코드만 추가)를 벗어난다.
9. **`ResolveModal.spec.tsx`의 `pnpm -r test` 병렬 실행 간헐 실패(2026-09-22, bug-triage 발견, §3.7.4)** — Low 등급 테스트 인프라 결함. `vi.useFakeTimers({shouldAdvanceTime:true})` 제거/`unmount()` 명시 호출/`afterEach`의 `vi.clearAllTimers()` 선행 등으로 결정성을 높이는 재작업이 필요하다. 상세 원인·라우팅은 §3.7.4와 `오류검출_프로세스.md` §6 참고.

### 4.7 다음 자동화 대상(FAQ/의도 매칭 고도화 완료 후 식별된 과제, 2026-09-22)

1. **`AC-N1-17` 테스트 태그 불일치 정정** — `nlu-rag-answering.integration.spec.ts`에 `"AC-N1-17"`로 태그된 케이스가 실제로는 재색인 API의 단일 `202` 응답만 확인하고, 요구사항의 AC-N1-17(동시 재요청 → `409 REINDEX_IN_PROGRESS`)은 검증하지 않는다. 기능 결함이 아니라 시험 위생 문제이므로 다음 회차에서 ① 태그를 정정하고 ② 실제 동시 재요청 케이스(첫 요청이 아직 진행 중인 상태에서 두 번째 요청 → 409)를 추가한다.
2. **1단계 임베딩 저하 모드/회로차단(AC-N1-9/10/11)** — `query-embedding.service.ts`(클라이언트 타임아웃 300ms, 연속 5회 실패 → 30초 open, `staleModel` 판정)에 전용 단위시험이 없다. `RagGateService`(`rag-gate.service.spec.ts`)와 동일한 패턴(작은 임계값으로 파라미터화해 경계값만 검증)으로 추가할 수 있다.
3. **색인 수명주기(AC-N1-13~15)** — `indexer.service.ts`의 `textHash` 기반 재임베딩 스킵(FR-N1-19), 저장 직후 즉시 재색인(FR-N1-18), 부분 색인 상태에서의 대화 정상 동작(S-9)에 전용 시험이 없다.
4. **`AnswerSettingsPage`/`AnswerSettingsPreviewPanel` 외 하위 컴포넌트 10종**(`ConnectionCheckButton`/`ConnectionStatusBadge`/`FallbackPolicyRadioGroup`/`IndexStatusBadge`/`NoDestructiveActionsNotice`/`ProviderLockedField`/`RagScopeFields`/`RagTimeoutField`/`ReindexButton`/`SimilarityThresholdField`/`ThresholdBandVisualizer`) 전용 렌더/axe 스캔 시험 부재(AC-N3-1/2/4/7/10) — `apps/web`은 이미 `jest-axe`가 도입돼 있으므로(§3.3) 다른 관리자 화면과 동일 패턴(`*.a11y.spec.tsx`)을 재사용할 수 있다.
5. **시뮬레이터 `useRag` 확장(AC-N2-25/26, AC-N3-7)** — `SimulatorPanel`/`CompareView`가 이번 그룹으로 확장한 매칭 근거 패널(상위 3후보 점수·구간 판정·2단계 사용 여부)과 "관리자 시뮬레이션은 기본적으로 RAG를 호출하지 않는다"는 구조를 스파이(`jest.spyOn`)로 명시 검증하는 케이스가 없다.
6. **정적 검사 확장** — `AC-N4-9`(신규 원시 SQL 0건)를 `rag-allowlist.spec.ts`와 같은 패턴(`$queryRaw`/`$executeRaw` 문자열 스캔)으로 자동화하면 코드 리뷰 의존도를 낮출 수 있다.
7. **NFR-P1~P7 정량 성능 시험** — 캐시 적중 시 500ms(P95), 유사도 계산 5,000벡터 20ms(P95), `PENDING` 응답 200ms(P95), 폴링 50ms(P95), 재색인 5,000건 10분 이내는 전부 k6 등 부하시험 도구 도입 시 재검토(§4.2 항목 3, §4.5 항목 6과 합류).
8. Playwright(또는 동급) E2E 도입 시 우선순위에 추가할 항목: "질문 전송 → PENDING → 폴링 → 완료"의 실제 네트워크 스택 종단 시나리오(§3.8.4), `AnswerSettingsPage` 저장→미리보기→연결점검 종단간 키보드 전용 시나리오(AC-N3-10과 결합).


### 4.8 다음 자동화 대상(학습 고도화 No.16/23 완료 후 식별된 과제, 2026-09-22)

1. **증강 금지어 필터 결함 수정 후 회귀 테스트 갱신** — §3.9.3의 Medium 결함(`containsBannedWord()`의 공백 전용 항목 오탐)이 수정되면 `validate-candidates.boundary.spec.ts`의 해당 테스트 기댓값을 `0`으로 반전해야 한다(현재는 결함 동작을 고정하는 회귀 센서).
2. **`AugmentationService`(generate/list/reject/capability) 전용 스펙 부재** — `AugmentationAcceptService`는 이번 시험 이전부터 전용 스펙이 있었지만, 실제 생성 오케스트레이션(임베딩 배치 호출·검증 파이프라인 연결·`TrainingJobQueue` 적재·capability 조회)을 다루는 `AugmentationService`는 여전히 전용 단위/통합 시험이 없다(AC-L1-1/8/10/12/13이 이 공백에 해당). 다음 순위로 신설 권고.
3. **`ClassifierTrainingService`/`ClassifierPredictService`/`DecompositionService` 전용 스펙 부재** — 순수 로직 조각(`logistic-regression`/`stale-judge`/`decompose`)은 단위 커버되지만, 이를 실제로 배선하는 서비스 3종(최소 데이터 조건 판정, `source:'CLASSIFIER'|'LEXICAL'` 분기, 조회 시점 계산)은 전용 시험이 없다(AC-L2-9/10/13~16/18). `RagAnswerService.spec.ts`(§3.8.1)와 같은 "협력자 전부 모킹 + 오케스트레이션 배선 검증" 패턴을 재사용할 수 있다.
4. **관리자 콘솔 화면 컴포넌트 시험 확장(AC-L3-2/3/5~10)** — `AugmentationSuggestionTable`의 "전체 선택 시 충돌 배지 제외" 로직, 검증 요약 문구, 요소분해 칩 키보드 조작, `axe` 스캔은 이번 시험에서 VIEWER 게이팅 2건만 추가했을 뿐 나머지는 FAQ/RAG 그룹(§4.7 항목 4)과 동일한 커버리지 공백 패턴이다.
5. **정적 검사 확장** — AC-L4-10(PII 마스킹 함수 1벌)·AC-L4-11(신규 원시 SQL 0건)을 `rag-allowlist.spec.ts`/`asset-write-sealing.spec.ts`와 같은 패턴으로 자동화하면 코드 리뷰 의존도를 낮출 수 있다(§4.7 항목 6과 동일한 다음 순위).
6. **분류기 학습 종단 정확도 검증** — §3.9.5에서 밝힌 대로 이번 통합 테스트는 "학습 성립 + 대화 무영향"만 검증한다. 실제 벡터로 클러스터가 분리된 데이터셋을 시딩해 `accuracy` 필드가 기대 범위 안에 드는지 확인하는 케이스는 `apps/api/src/classifier/eval/`의 오프라인 산출물과 역할이 겹치지 않도록 범위를 신중히 설계해야 한다.
7. Playwright(또는 동급) E2E 도입 시 우선순위에 추가할 항목: 의도 상세 "예문 늘리기"→생성 대기→제안 검토→승인 종단간 키보드 시나리오, 학습현황의 요소분해 칩 편집→반영 종단 시나리오(§4.2~4.7에서 이월된 동일 제약과 합류).

## 5. 책임

`test-automation` 에이전트가 `시험항목.md`을 기준으로 자동화 스크립트 작성·유지보수와 커버리지 추적을 함께 담당한다.

## 6. 검증/품질 고도화(No.19/20) 그룹 — 실행 결과 추가(2026-09-23)

> §1의 "대화 품질 회귀 시험" 행이 가리키던 No.19 "대화검증시스템&TC테스트"가 이제 구현·시험 완료됐다. 백엔드 코드(`apps/api/src/validation/**`)는 이번 시험 회차 이전에 이미 구현이 끝나 있었고, 아래는 이번 시험에서 **새로 추가한 테스트**만 기록한다(기존에 통과하던 `judge-test-case.spec.ts`/`compare-runs.spec.ts`/`summarize-run.spec.ts`/`env-fingerprint.spec.ts`/`parse-test-case-row.spec.ts`/`test-case-import.service.spec.ts`/`test-run.service.spec.ts`/`test-run-status.sink.spec.ts`/`validation-sealing.spec.ts`는 재작성 없이 그대로 유지). CI에서 이 기능 자체를 헤드리스로 돌리는 §1의 원래 구상(엑셀 시드 → TC 세트 → 자동 실행 → 회귀 판정을 파이프라인에 연결)은 §2의 CI 워크플로 자체가 아직 없어(2026-09-19 기준 상태 불변) 여전히 다음 순위다 — 이번 회차는 **그 기능 자체의 정확성**을 apps/api Jest 스위트로 검증했다.

### 6.1 `apps/api` — 신규 4파일 + 기존 2파일 확장(23+3=26 tests)

| 계층 | 위치 | 검증 항목 |
|---|---|---|
| 단위 | `apps/api/src/validation/run/test-run-rag.service.spec.ts`(신규, 9 tests) | RAG(2단계) 취급 전 조건 조합 — band 아님/ragEnabled=false/useRag=false/상한초과/게이트실패/정상성공/PII마스킹/네트워크오류 시 게이트 release 보장/미설정 |
| 단위 | `apps/api/src/validation/run/test-run-overlay.builder.spec.ts`(신규, 8 tests) | M2 오버레이 합성 로직 — PENDING만 채택, stale(status/modelId) 제외, currentModelId undefined 시 modelId 검사 생략, 삭제된 의도/제안 제외, 정규화 기준 중복 스킵, 20건 상한 |
| 단위 | `apps/api/src/validation/run/test-run.executor.spec.ts`(기존 3 -> 4 tests) | 신규: RAG 실행당 상한(TEST_RUN_RAG_MAX_CALLS) 강제 — underBudget 인자가 순서대로 true N개/false M개로 전달되는지, ragCallCount가 정확히 기록되는지 |
| 단위 | `apps/api/src/common/auth/lib/permission-matrix.spec.ts`(기존 4 -> 6 tests) | 신규: Permission 유니온이 정확히 15종(값 목록까지 고정) + simulation:write가 EDITOR/ADMIN에는 있고 VIEWER에는 없음 |
| 통합(행위 기반, 최중요) | `apps/api/src/embedding/query-embedding-cache-isolation.spec.ts`(신규, 1 test) | AC-V4-3 — 실제 QueryEmbeddingService(운영 캐시) + 실제 TestRunExecutor를 같은 EmbeddingProviderFactory로 연결, 900건 운영 캐시 시딩 후 2,000건 TC 실행, 최초 900건이 여전히 캐시 적중함을 provider 재호출 횟수로 증명. 의도적으로 validation 디렉터리 밖(embedding 모듈)에 배치 — 안에 두면 validation-sealing.spec.ts의 QueryEmbeddingService 참조 0건 검사가 정당하게 실패한다 |
| 통합(HTTP 계약 + 행동적 증거) | `apps/api/src/integration/validation-regression.integration.spec.ts`(신규, 5 tests) | (1) AC-V2-10 로그·큐 오염 0(ConversationLog/UnansweredQuestion/RagCallLog baseline 유지) (2) AC-V2-12 결정론(동일 세트 2회 실행 결과 완전 일치) (3) M2 쓰기 0건(AugmentationSuggestion/Intent/Keyword 불변) (4) AC-V4-9 챗봇 영구삭제 4테이블 동반 삭제 재확인 (5) VIEWER 21핸들러 전수 게이팅(쓰기 12개 -> 403, 읽기 9개 -> 200, 개수 자체를 고정) |

- 신규 통합 테스트는 `EMBEDDING_BASE_URL`을 설정하지 않는다(규칙 매칭만) — AC-V4-10(환경변수 0개로 정상 동작)을 동시에 재확인하면서, 이 그룹이 "새 모델을 적재하지 않는다"(요구사항 §4.5)는 설계 전제와 일치하는 가장 단순한 재현 환경이다.
- `query-embedding-cache-isolation.spec.ts`는 `MockEmbeddingProvider`(결정론적 해시 임베더)만으로 2,900회 임베딩 호출을 3초 내에 처리한다 — 실제 ml-worker나 HTTP 목서버 없이도 J-8(캐시 오염 금지)의 행위 기반 증거를 짧은 시간 안에 재현 가능함을 보였다.

### 6.2 이번 시험에서 발견한 결함

없음. `test-run-overlay.builder.spec.ts`·`test-run-rag.service.spec.ts`·`query-embedding-cache-isolation.spec.ts`·`validation-regression.integration.spec.ts` 총 26개 신규 테스트가 전부 첫 시도에 통과했다 — 사전 구현이 설계서(ADR-0029/0030)의 봉인·격리·판정 규칙을 정확히 따랐음을 의미한다.

### 6.3 회귀 확인(변경 없음, 이번 시험에서 재실행만)

| 스위트 | 결과 |
|---|---|
| `pnpm --filter @chat-bot/api test`(Jest) | 87 suites / 766 tests, 전부 Pass(신규 4파일 23 tests + 기존 2파일 확장 3 tests 포함 — 시험 전 83 suites/740 tests) |
| `pnpm --filter @chat-bot/web test`(Vitest) | 41 files / 179 tests, 전부 Pass(변경 없음 — 이번 그룹은 프런트 신규 테스트를 추가하지 않았다) |
| `apps/ml-worker`(pytest) | 변경 없음(이 그룹은 ml-worker 파일을 건드리지 않음 — 미재실행) |

### 6.4 다음 자동화 대상(검증/품질 고도화 No.19/20 완료 후 식별된 과제, 2026-09-23)

1. **M2 오버레이 예문의 실제 임베딩 효과(AC-V3-7)** — 이번 통합 테스트는 `EMBEDDING_BASE_URL` 미설정(규칙 매칭만) 환경이라 "오버레이 예문이 임베딩되어 B의 top1Score가 A보다 높다"를 재현하지 못한다. `learning-augmentation.integration.spec.ts`처럼 mock 임베딩 HTTP 서버를 별도로 띄우는 통합 테스트, 또는 `MockEmbeddingProvider`를 직접 주입하는 조립형(NestJS 앱 없이 서비스만 조립) 시험 중 하나를 다음 순위로 추가한다.
2. **`TestRunCompareService`(M1 비교 서비스) 전용 스펙 부재** — 5분류 순수 함수(`compare-runs.spec.ts`)는 충분히 커버되지만, 비교 가능성 사전 판정(`TEST_RUN_NOT_COMPARABLE` — 다른 세트, 취소/실패 실행 기준선 불가, 챗봇 교차 404)과 환경지문 강한 경고 배지 배선은 서비스 레벨 시험이 없다.
3. **AC-V2-13/14·EX-V-1(정상 -> 저하 전환, 서버 재시작 재현)** — `learning-augmentation.integration.spec.ts`의 앱 재부팅(app1 -> app2) 패턴을 `TestRunService.onModuleInit()`(고아 실행 정리)에도 재사용하면 비용 대비 효과가 크다.
4. **감사로그 조회 케이스(AC-V4-7/8)** — `TestCaseSet` CRUD/임포트가 실제로 감사 레코드를 남기고 실행/비교는 남기지 않는지를 `security-audit.integration.spec.ts`의 기존 "AC-13-*" 패턴으로 1~2케이스 추가하면 해소된다.
5. **관리자 콘솔 화면(검증 5뷰) 컴포넌트/axe 시험** — `apps/web/src/pages/chatbot-detail/validation/**`(세트 목록/상세/실행 목록/실행 결과/비교 결과)는 이번 시험에서 신규 프런트 테스트를 추가하지 않았다. FAQ/RAG·학습 고도화 그룹과 동일한 커버리지 공백 패턴(§4.7~4.8)이므로 같은 순위로 다음 회차에 편입한다.
6. **P95 성능(AC-V4-4, NFR-VP2)** — k6 등 부하시험 도구 도입 시 §4.2 항목 3과 합류.
