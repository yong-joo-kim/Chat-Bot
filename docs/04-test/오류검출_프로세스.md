# 오류검출 및 내용보완 프로세스 v1.0

## 1. 결함 분류 기준

| 등급 | 정의 | 예시 |
|---|---|---|
| Critical | 서비스 전체 장애, 데이터 유실/손상, 보안 취약점 | 대화 엔진 응답 불가, 권한 우회로 타 챗봇 데이터 접근 |
| High | 핵심 기능 불가, 우회 방법 없음 | 대화그래프 저장 실패, 채널 Webhook 수신 불가 |
| Medium | 기능은 동작하나 일부 시나리오에서 오류/부정확 | 특정 슬롯필링 조합에서 컨텍스트 유실 |
| Low | UI/UX 사소한 문제, 문구 오류, 테스트 인프라 안정성 문제(프로덕션 코드에는 영향 없음) | 색상 대비 기준 미달, 오탈자, 병렬 실행 시에만 재현되는 간헐적 테스트 실패 |

## 2. 검출 채널

1. 자동시험 실패(CI) — `자동시험_전략.md` 파이프라인에서 자동 수집
2. 수동 시험(`시험항목.md` 체크리스트 수행 중 발견)
3. 대화 품질 모니터링(운영 중 미응답률/오응답 급증 알림 — No.2 대시보드, No.24 하이브리드CS 연계)
4. 사용자/상담원 피드백(운영 단계)

## 3. 처리 절차

```
검출 → 등급 분류(위 기준) → 재현 확인 → 원인 분석 → 수정 → 회귀시험(관련 시험항목 재실행) → 종료
```

- Critical/High는 발견 즉시 담당 에이전트/개발자에게 할당, 수정 전까지 관련 배포 보류(`docs/05-ops/자동배포.md` 게이트 연계)
- Medium/Low는 스프린트 단위로 backlog화하여 우선순위 조정

## 4. 내용보완(문서 갱신) 원칙

- 시험 중 발견된 요구사항 누락/오해는 `docs/01-requirements/기능요구사항.md`을 직접 갱신하고, 변경 이력을 해당 문서 내 "교차검증 메모" 섹션에 이어서 기록한다.
- 설계 변경이 필요한 결함은 `docs/02-spec/개발명세서.md`를 갱신한다.
- 문서 갱신 없이 코드만 수정하는 것을 금지 — 요구사항/설계와 실제 구현의 괴리를 방지한다.

## 5. 책임

`bug-triage` 에이전트가 결함 등급 분류와 라우팅을 담당하고, 각 기능 담당 에이전트(system-architect, ui-designer, backend-implementer, ml-engineer, frontend-implementer 등)가 원인 영역에 따라 문서/코드를 보완한다.

## 6. 결함 이력(bug-triage 기록)

이 절은 `bug-triage`가 판정한 결함을 등급·원인·라우팅과 함께 시간순으로 기록한다(신규 발견은 하단에 이어서 추가). 요구사항/설계 문서 갱신이 필요한 건은 §4 원칙에 따라 해당 문서에 직접 반영하고 이 표에는 결과만 요약한다.

| 발견일 | 결함 요약 | 등급 | 원인 영역 / 소관 그룹 | 라우팅 | 상태 |
|---|---|---|---|---|---|
| 2026-09-22 | `apps/web/src/pages/learning/ResolveModal.spec.tsx`(No.14~15 "기본 통계/학습현황" 그룹, 커밋 `bd4ae05`)가 `pnpm -r test`(모노레포 전체 동시 실행)로 돌릴 때만 간헐적으로 `Uncaught Exception: Cannot read properties of undefined (reading 'then')`(`ResolveModal.tsx:77`, 디바운스 `setTimeout` 콜백이 테스트 종료 후 발화)로 실패. 단독 실행(`vitest run ResolveModal.spec.tsx` 10/10, `pnpm --filter @chat-bot/web test` 170/170)은 항상 통과. "FAQ/의도 매칭 고도화(No.6/9/18/30 부분)" 그룹 test-automation 단계에서 발견됐으나 이 그룹은 해당 파일을 수정하지 않았음 | Low — 테스트 인프라 안정성 문제로 판정. `ResolveModal.tsx:67~87`의 디바운스 `useEffect`는 `return () => clearTimeout(timer)` cleanup을 정상 반환해(코드 리뷰로 확인) 실제 브라우저/네이티브 타이머 기준으로는 언마운트·의존성 변경 시 타이머가 확실히 취소된다 — 프로덕션 코드 결함은 확인되지 않음. 에러 메시지("reading 'then' of undefined")는 모의 객체(`mockIntentsList`)가 리셋된 뒤 값이 없어 `undefined`를 반환했다는 뜻으로, 실 서버 호출(항상 Promise 반환)에서는 발생할 수 없는 형태 — `vi.useFakeTimers({shouldAdvanceTime:true})`+병렬 실행 시 CPU 경합이 겹쳐 이전 케이스의 타이머가 `vi.useRealTimers()`/`mockReset()` 시점과 경합하는 테스트 격리 문제로 추정(상세 근거는 `자동시험_전략.md` §3.7.4) | 테스트 코드(`ResolveModal.spec.tsx`의 fake timer 설정) — No.14~15 그룹 소관. 요구사항(`기능요구사항.md`)/설계(`개발명세서.md`) 변경 불필요 | frontend-implementer(No.14~15 그룹 재작업 백로그로 전달 — `unmount()` 명시 호출/`shouldAdvanceTime` 제거/`afterEach`에 `vi.clearAllTimers()` 선행 등 후보) | 기록만, 코드/스펙 미수정(등급 Low이므로 `자동배포.md` 배포 게이트 잠금 불필요 — 참고로 3단계 CI 자체도 아직 미착수 상태) |
