# 시험항목 (기능별 테스트 케이스)

> `시험계획.md`의 완료 기준에 따라 각 기능별 대표 케이스를 정의한다. 실제 구현 시 `test-automation` 서브에이전트가 세부 케이스를 확장한다.

## 기본기능

| ID | 기능 | 시나리오 | 예상 결과 |
|---|---|---|---|
| TC-01 | 챗봇 리스트/그룹 관리(No.1) | 그룹 생성 → 챗봇 3개 생성 → 그룹 복사(AC-1-9), 챗봇 CRUD/복사/보관/영구삭제/페이지네이션/검색(AC-1-1~1-16, EX-1-1~1-10) — 세부 케이스는 아래 "챗봇 운영관리 상세 시험항목" 참고 | 복사된 그룹에 챗봇 3개 모두 동일 설정(DRAFT)으로 존재. 상세 AC별 예상결과는 `docs/requirements/chatbot-operations.md` §7 AC-1 참고 |
| TC-02 | 대시보드(No.2) | 대화로그 100건(sample-support-bot) 적재 후 대시보드 조회(AC-2-1~2-4, 2-7~2-9, 2-11~2-12), 로그 0건(empty-dashboard-bot) 빈 상태 조회(AC-2-5, AC-2-6) | 접속수/응답률/미응답률이 로그와 일치, 빈 상태는 오류가 아닌 EmptyState로 표시 |
| TC-03 | 챗봇 기본설정(No.3) | 이름/아바타/소개/slug 부분 수정, slug 형식·예약어·중복 검증, slug 변경 경고 모달, 저장하지 않은 변경사항 가드(AC-3-1~3-9) | 필드별 인라인 오류 표시, slug 변경 시 경고 모달 확인 후에만 저장, 탭 이동/TopBar 이동 시 가드 동작 |
| TC-04 | 스킨/임베드 설정(No.4) | 주색상/헤더문구/로고 설정, 대비 경고 배지, 기본값 복원, PC/모바일 임베드 코드 생성·복사, slug 변경 시 임베드 코드 갱신(AC-4-1~4-11) | 저장 전 실시간 미리보기 반영, 대비 4.5:1 미만 시 경고(저장 차단 아님), 임베드 코드가 slug 변경을 즉시 반영 |
| TC-05 | 대화그래프 빌더(No.5) | 텍스트+버튼+컨텍스트폼 노드로 시나리오 구성 후 저장(AC-5-1~5-14, AC-E-1~12) — 세부 케이스는 아래 "대화 설계 상세 시험항목" 참고 | 저장/재조회 시 노드·연결 정보 손실 없음. 상세 AC별 예상결과는 `docs/requirements/dialogue-design.md` §7 AC-5/AC-E 참고 |
| TC-06 | 의도·키워드 관리(No.6) | 엑셀 대량 업로드(예문 500개), 의도/키워드 CRUD(AC-6-1~6-10, AC-6B-1~6B-10, M2) | 전량 등록, 실패 행은 오류 사유와 함께 리포트. 검증(dry-run) 예고와 커밋 결과가 정확히 일치(M2) |
| TC-07 | 동음이의어/다의어 사전(No.7) | "배"(과일/신체/교통수단) 등록 후 발화 매칭(AC-7-1~7-7) | 컨텍스트에 따라 올바른 의도로 분기 |
| TC-08 | 컨텍스트(슬롯필링)(No.8) | 커피주문형 순차 슬롯 대화(음료→사이즈→옵션)(AC-8-1~8-11) | 중간 이탈 후 재진입 시 이전 슬롯 값 유지 |
| TC-09 | FAQ 관리(No.9) | FAQ 200개 등록 후 자동완성 검색(AC-9-1~9-8) | 200ms 이내 상위 5개 후보 반환 |
| TC-10 | 응답 테스트/시뮬레이션(No.10) | 실시간 대화 테스트, 저장 전 미리보기(오버레이), 저장본 vs 미저장 변경 비교(AC-10-1~17, AC-10B-1~12) — 세부 케이스는 아래 "품질/채널 상세 시험항목" 참고. 원래 문구("운영 vs 학습중 비교")는 J-1 결정에 따라 이번 Phase 범위가 아니다(`docs/requirements/quality-channel.md` §4.1.3) | 오버레이는 DB를 변경하지 않고, A/B 비교는 달라진 턴만 DIFFERENT로 표시. 상세 AC별 예상결과는 `docs/requirements/quality-channel.md` §7 AC-10/AC-10B 참고 |
| TC-11 | 다양한 채널 제공(No.11) | WEB 채널 활성화 + 공개 대화 API 종단 연결(`apps/widget`), 나머지 7종은 설정만 저장(CONFIG_ONLY, AC-11-1~12, AC-P-1~15, AC-W-1~18) — 세부 케이스는 아래 "품질/채널 상세 시험항목" 참고. J-2 결정에 따라 이번 Phase는 WEB만 종단 동작한다 | WEB 채널로 실제 대화 가능, 나머지는 활성화 시도 시 409(CHANNEL_NOT_IMPLEMENTED). 상세 AC별 예상결과는 `docs/requirements/quality-channel.md` §7 AC-11/AC-P/AC-W 참고 |
| TC-12 | 회원·권한·보안 | 뷰어 권한 계정으로 설정 변경 시도 | 403 반환, 화면에서 편집 UI 비활성 |
| TC-13 | 이력관리 | 의도 삭제 → 이력조회 | 삭제 주체/시각/이전값이 기록 |
| TC-14 | 기본 통계 | 주간 통계 조회 | 일별 합계 = 주간 합계 |
| TC-15 | 학습현황(관리자 보조 재학습) | 미응답 질문 선택 → 의도명 입력 → 저장 | 해당 의도 예문에 반영, 재학습 Job 생성 |

### 챗봇 운영관리(No.1~4) 상세 시험항목 — AC/EX 매핑

> 대상 문서: `docs/requirements/chatbot-operations.md` §7(AC)/§8(EX). "자동화 위치"는 실제 존재하는 테스트 파일 경로다(2026-09-19 기준). 신규 기능이 아니라 회귀 방지를 위해 유지되는 케이스이므로 향후 변경 시 이 표도 함께 갱신한다.

| AC/EX | 시나리오 요약 | 자동화 위치 | 구분 |
|---|---|---|---|
| AC-1-1 | 그룹 생성 시 201 + chatbotCount:0 | `apps/api/src/integration/chatbot-operations.integration.spec.ts` | 통합 |
| AC-1-2 | 챗봇 생성 시 status=DRAFT, skin 기본값 | 〃 | 통합 |
| AC-1-3 / EX-1-1 | 중복 slug → 409 DUPLICATE_SLUG | 〃 | 통합 |
| AC-1-4 / EX-1-2 | slug 형식 위반 → 400 + field:slug | 〃 | 통합 |
| EX-1-6 | 존재하지 않는 groupId로 생성 → 404 | 〃 | 통합 |
| AC-1-5 / EX-1-3 | 잔여 챗봇 그룹 삭제 → 409 GROUP_NOT_EMPTY + 보존 | 〃(API) / `apps/web/src/pages/ChatbotListPage.spec.tsx`(UI 배너·모달 유지) | 통합+컴포넌트 |
| AC-1-6 / EX-1-4 | 빈 그룹 삭제 → 204 | `chatbot-operations.integration.spec.ts` | 통합 |
| AC-1-7 / AC-1-8 / EX-1-7 | 챗봇 복사 slug 파생(-copy, -copy-2, 50자 클램프) | 〃 + `apps/api/src/chatbots/lib/slug.util.spec.ts`(순수함수) | 통합+단위 |
| AC-1-9 | 그룹 복사 시 소속 챗봇 N개 함께 복제, 전부 DRAFT | `chatbot-operations.integration.spec.ts` | 통합 |
| AC-1-10 | 챗봇 보관(DELETE)→ARCHIVED, 기본필터 제외/포함 토글 | 〃 | 통합 |
| AC-1-11 | ARCHIVED+로그보유 챗봇 영구삭제 → 409 CHATBOT_HAS_CHILDREN / ARCHIVED 아닌 챗봇 영구삭제 → 409 CHATBOT_NOT_ARCHIVED | 〃 | 통합 |
| AC-1-12 / EX-1-8 | ARCHIVED 챗봇 설정 수정 → 409 CHATBOT_ARCHIVED, UI 편집 비활성 | 〃(API) / `ArchivedBanner` 렌더는 수동 확인(§ UI/접근성 시험 하단 비고) | 통합+수동 |
| AC-1-13 | ARCHIVED→ACTIVE 직접전환 400, →DRAFT 200 | `chatbot-operations.integration.spec.ts` + `status-transition.spec.ts` | 통합+단위 |
| AC-1-14 | pageSize=20, 챗봇 30개 → 1페이지 20건/total 30, 2페이지 10건 | `chatbot-operations.integration.spec.ts` | 통합 |
| AC-1-15 | q="주문" 검색 → 일치 항목만 반환 | 〃 | 통합 |
| AC-1-16 / EX-1-6 | 존재하지 않는 groupId로 그룹 이동 → 404 | 〃 | 통합 |
| EX-1-10 | 동시 수정(last-write-wins) | 자동화 없음(Phase 1 정책 문서화만, D-4) | 미자동화 |
| AC-2-1~2-4, EX-2-2, EX-2-3 | 응답률/미응답률/인기질문 정규화·동률처리 | `apps/api/src/stats/lib/dashboard-aggregator.spec.ts` | 단위 |
| AC-2-5 / AC-2-9 | 로그 0건 챗봇 200+0값, 존재하지 않는 챗봇 404 | `chatbot-operations.integration.spec.ts` | 통합 |
| AC-2-6 / EX-2-1 | 빈 상태 화면(오류 아님, 임베드 바로가기 CTA) | `apps/web/src/pages/chatbot-detail/DashboardTab.spec.tsx` | 컴포넌트 |
| AC-2-7 / AC-2-8 / EX-2-4 | 기본 기간(최근7일), 기간 역전/366일초과 → 400 | `apps/api/src/stats/lib/dashboard-period.spec.ts` + 통합(FR-2-8) | 단위+통합 |
| AC-2-10 | ARCHIVED 챗봇 대시보드 조회 허용(누적 지표) | `chatbot-operations.integration.spec.ts` | 통합 |
| AC-2-11 | 로딩 스켈레톤 → "조회기간·집계건수" 완료 배지 전환 | `DashboardTab.spec.tsx` | 컴포넌트 |
| AC-2-12 | 새로고침 연타 시 요청 1회만 발생 | `DashboardTab.spec.tsx` | 컴포넌트 |
| EX-2-5 | 집계 타임아웃(503) → ErrorState+재시도, 캐시 미노출 | 자동화 없음(백엔드 타임아웃 강제 재현 어려움) — 코드 리뷰로 로직 확인, 수동 시나리오 권고 | 미자동화 |
| AC-3-1 | name만 부분수정 시 slug/skin/status 보존 | `chatbot-operations.integration.spec.ts` | 통합 |
| AC-3-2 | name 빈 문자열 → 400 + field:name | 〃 | 통합 |
| AC-3-3 | description 501자 → 400 | 〃 | 통합 |
| AC-3-4 / NFR-S3 | avatarUrl에 `javascript:` 스킴 → 400 | 〃 | 통합 |
| AC-3-5 | slug 예약어(admin) → 400 + 예약어 안내 | 〃 | 통합 |
| AC-3-6 | slug 중복확인 디바운스 400ms, 사용불가 시 저장 비활성 | `apps/web/src/components/SlugAvailabilityField.tsx` 동작은 `SettingsTab.spec.tsx`에서 간접 검증(mock) — 디바운스 타이밍 자체의 전용 단위시험은 미작성 | 부분 자동화 |
| AC-3-7 / FR-3-8 / EX-3-3 | slug 변경 시 경고 모달 → 확인해야 PATCH 전송 | `apps/web/src/pages/chatbot-detail/SettingsTab.spec.tsx` | 컴포넌트 |
| AC-3-8 / FR-3-12 | 변경사항 있는 상태에서 이동 시 확인(TopBar 포함) | `apps/web/src/components/TopBar.spec.tsx` + `SettingsTab.spec.tsx` | 컴포넌트 |
| AC-3-9 | 저장 성공 토스트 + 공개 URL 미리보기 갱신 | `SettingsTab.spec.tsx`("변경 후 저장" 테스트) | 컴포넌트 |
| AC-4-1 / NFR-P3 | 저장 전 미리보기 즉시 반영 | 자동화 없음(클라이언트 상태 렌더는 `SkinPreviewPanel` 수동 확인) — 후속 컴포넌트 시험 후보 | 미자동화 |
| AC-4-2 | primaryColor 형식 위반 → 400 | `chatbot-operations.integration.spec.ts` | 통합 |
| AC-4-3 / FR-4-6 | 대비 4.5:1 미만 경고 배지(저장 차단 아님) | `apps/web/src/lib/contrast.spec.ts`(WCAG 계산) + `apps/web/src/pages/chatbot-detail/ContrastWarningBadge.spec.tsx`(렌더) | 단위+컴포넌트 |
| AC-4-4 | headerTitle 51자 → 400 | `chatbot-operations.integration.spec.ts` | 통합 |
| AC-4-5 | 스킨 저장 후 재조회 시 객체 형태로 일치 | 〃 | 통합 |
| AC-4-6 | 기본값 되돌리기 확인 후 스킨 초기화 | `apps/api/src/chatbots/lib/skin.util.spec.ts`(mergeSkin 리셋 로직) | 단위 |
| AC-4-7 | 임베드 탭에 PC/모바일 스니펫+공개URL, 복사 버튼 | `chatbot-operations.integration.spec.ts`(API) | 통합 |
| AC-4-8 / EX-4-2 | 복사 성공 피드백 / 클립보드 실패 시 폴백 | 자동화 없음(`CopyButton`은 `navigator.clipboard` mock 필요) — 후속 컴포넌트 시험 후보 | 미자동화 |
| AC-4-9 / FR-4-13 | slug 변경 후 임베드 코드 조회 시 새 slug 반영(조회시점 생성) | `chatbot-operations.integration.spec.ts` | 통합 |
| AC-4-10 | DRAFT 챗봇 임베드 탭 주의문구 + 코드 확인·복사는 가능 | 자동화 없음(`DraftEmbedNotice` 렌더 조건은 코드 리뷰로 확인) | 미자동화 |
| AC-4-11 / NFR-S2 | headerTitle에 `<script>` 저장 시 텍스트로만 렌더링 | 자동화 없음(React 기본 이스케이프에 의존 — `SkinPreviewPanel` 전용 컴포넌트 시험 후보) | 미자동화 |
| AC-5-1 | 전 응답이 대응 zod 스키마 파싱 통과 | `chatbot-operations.integration.spec.ts`(각 케이스에서 `*Schema.parse` 사용) | 통합 |
| AC-5-2 | 검증오류 응답에 details[].field 포함 | 〃 + `apps/web/src/lib/apiErrorHelpers.ts` 소비측은 `SettingsTab.spec.tsx`/`ChatbotListPage.spec.tsx`에서 간접 검증 | 통합+컴포넌트 |
| AC-5-3 | 그룹생성 등 플로우를 키보드(Tab/Enter)만으로 완료 | `apps/web/src/pages/ChatbotListPage.spec.tsx`("AC-5-3" 테스트, 클릭 없이 Tab+Enter만 사용) — **전 구간(그룹트리→리스트→설정폼→저장) end-to-end 키보드 시험은 Playwright 등 E2E 인프라 도입 전까지 수동 확인 병행 필요** | 컴포넌트+수동 |
| AC-5-4 | axe 스캔 대비 4.5:1 미만 요소 0건 | `apps/web/src/pages/ChatbotListPage.a11y.spec.tsx`(jest-axe, `color-contrast` 룰은 jsdom 한계로 비활성화 — 하단 "알려진 한계" 참고) | 컴포넌트(부분) |
| AC-5-5 | 모달 Esc 취소 시 무변경 + 포커스 트리거 복귀 | `apps/web/src/components/Modal.spec.tsx` + `SettingsTab.spec.tsx` | 컴포넌트 |

### 대화 설계(No.5~9) 상세 시험항목 — AC/EX 매핑

> 대상 문서: `docs/requirements/dialogue-design.md` §7(AC)/§8(EX). "자동화 위치"는 실제 존재하는 테스트 파일 경로다(2026-09-20 기준). 요구사항→설계→백엔드→프런트엔드 구현과 code-reviewer 리뷰(H1 삭제차단 배너 바로가기, H2 참조종류 라벨 정확성, H3 노드 다중필터 서버위임, M2 대량업로드 검증-커밋 일치 — 전부 재작업 완료)를 마친 뒤 시험한 결과다. 신규 변경 시 이 표도 함께 갱신한다.

| AC/EX | 시나리오 요약 | 자동화 위치 | 구분 |
|---|---|---|---|
| AC-6-1 | 예문 포함 의도 생성 → 201 + 예문 개수 일치 | `apps/api/src/integration/dialogue-design.integration.spec.ts` | 통합 |
| AC-6-2 | 공백 차이만 있는 이름 재생성 → 409 DUPLICATE_NAME | 〃 | 통합 |
| AC-6-3 | 정규화 기준 중복 예문 자동 제거 + deduplicatedCount | 〃 | 통합 |
| AC-6-4 | 공백만 있는 예문 → 400 | 〃 | 통합 |
| AC-6-5 | 다른 의도가 이미 가진 예문 저장 → 201 + conflicts 포함 | 〃 | 통합 |
| AC-6-6 | 노드가 참조 중인 의도 삭제 → 409 INTENT_IN_USE + 참조 노드 목록 | 〃 | 통합 |
| AC-6-7 | 참조 없는 의도 삭제 → 204 | 〃 | 통합 |
| AC-6-8 | 일괄 삭제 중 1건 참조 시 전체 롤백(부분 삭제 금지) | 〃 | 통합 |
| AC-6-9 | 다른 키워드가 가진 동의어 등록 → 409 SYNONYM_CONFLICT | 〃 | 통합 |
| AC-6-10 | 예문 500개 초과 → 400 | 자동화 없음(순수 함수 `dedupeExamples`/서비스 상한 체크는 코드 리뷰로 확인, 500건 픽스처 생성 비용 대비 우선순위 낮음) | 미자동화 |
| FR-6-18 | 노드가 참조 중인 키워드 삭제 → 409 KEYWORD_IN_USE | `dialogue-design.integration.spec.ts` | 통합 |
| EX-R-2 / **H2** | 동음이의어가 연결한 의도 삭제 → 409 메시지에 "동음이의어" 포함(프런트 `resolveBlockedRefKind` 판별 근거) | `dialogue-design.integration.spec.ts`(서버 메시지) + `apps/web/src/pages/dialogue/components/DeleteBlockedBanner.spec.tsx`(판별·렌더·이동) + `IntentsKeywordsPage.spec.tsx`(종단 통합) | 통합+컴포넌트 |
| EX-R-3 / **H2** | 컨텍스트 슬롯(KEYWORD 타입)이 참조 중인 키워드 삭제 → 409 메시지에 "컨텍스트" 포함 | 〃 | 통합+컴포넌트 |
| EX-R-5 / **H1** | DIALOG_MOVE 대상 노드 삭제 차단 + 배너 링크 클릭 시 노드 편집 화면 이동 | `dialogue-design.integration.spec.ts`(AC-5-11) + `DeleteBlockedBanner.spec.tsx`(kind=node) + `NodesListPage.spec.tsx` | 통합+컴포넌트 |
| AC-6B-1 | 템플릿 다운로드 헤더 형식(`의도명,예문,설명`) | 자동화 없음(정적 헤더 상수 `INTENT_TEMPLATE_HEADERS`는 `csv-writer.ts`에 고정 — 코드 리뷰로 확인) | 미자동화 |
| AC-6B-2 / AC-6B-3 | 검증은 저장하지 않음(dry-run) + SKIP_INVALID·MERGE 커밋만 실제 반영 | `dialogue-design.integration.spec.ts` | 통합 |
| AC-6B-4 | ABORT_ON_ERROR 커밋 → 400 + 한 건도 반영 안 됨 | 〃(API) + `BulkImportModal.spec.tsx`(전체취소 배너 렌더) | 통합+컴포넌트 |
| AC-6B-5 | 커밋 중 DB 오류 시 트랜잭션 전체 롤백 | 자동화 없음(실제 DB 오류를 결정적으로 재현하기 어려움 — `$transaction` 사용은 코드 리뷰로 확인) | 미자동화 |
| AC-6B-6 | 만료된 importToken 커밋 → 400 IMPORT_TOKEN_EXPIRED | `dialogue-design.integration.spec.ts`(API) + `BulkImportModal.spec.tsx`(재검증 배너) | 통합+컴포넌트 |
| AC-6B-7 | 6MB/5,001행 파일 → 400 IMPORT_TOO_LARGE | 자동화 없음(대용량 파일 생성 비용 — `IMPORT_LIMITS` 상수 검증은 코드 리뷰) | 미자동화 |
| AC-6B-8 | 수식(`=1+1`) 셀 값 내보내기 시 이스케이프 | 자동화 없음(`escapeCsvCell` 자체는 `bulk-import.ts`에 있으나 전용 단위시험 없음 — 후속 과제) | 미자동화 |
| AC-6B-9 | UTF-8 BOM 유무와 무관하게 한글 정상 처리 | 자동화 없음(`hasEncodingAnomaly` 코드 리뷰로 확인) | 미자동화 |
| AC-6B-10 | 업로드 버튼 연타 시 요청 1회만 | `BulkImportModal.spec.tsx`("검증 중에는 버튼 비활성화" 테스트) | 컴포넌트 |
| M2 | 키워드 동의어 충돌 시 검증 리포트(그룹 전체 스킵 예고)와 실제 커밋 결과(createdItems/skippedRows) 일치 | `apps/api/src/dialogue-common/import/lib/import-planner.spec.ts`(순수 함수) + `dialogue-design.integration.spec.ts`("M2" 테스트, HTTP 왕복) | 단위+통합 |
| AC-7-1 | 의미 1건 → 400, 2건 이상 → 201 | `dialogue-design.integration.spec.ts` + `packages/shared-types`의 `CreateHomonymSchema` superRefine(스키마 자체 검증) | 통합 |
| AC-7-2 | 두 의미에 동일 문맥 힌트 → 400 | 〃 | 통합 |
| AC-7-3 | 존재하지 않는 intentId → 404 | 〃 | 통합 |
| AC-7-4 | ASK 정책, 힌트 없는 입력 → 되묻기(BUTTON) | `packages/dialogue-engine/src/homonym.spec.ts` + `dialogue-design.integration.spec.ts`(API `/homonyms/test`) | 단위+통합 |
| AC-7-5 | 문맥 힌트 있으면 의미 확정 + 연결 의도 포함 | 〃 | 단위+통합 |
| AC-7-6 | IGNORE 정책은 모호해도 되묻지 않음 | 〃 | 단위+통합 |
| AC-7-7 / EX-R-2 | 사전이 참조하는 의도 삭제 → 409 | `dialogue-design.integration.spec.ts`(EX-R-2와 동일 케이스) | 통합 |
| FR-7-9 | 참조 제약 없는 사전 항목 삭제 → 항상 204 | `dialogue-design.integration.spec.ts` | 통합 |
| FR-7-10 | 의미 반복 블록 추가/삭제/순서변경(위/아래 버튼) | 자동화 없음(`HomonymEditModal` 전용 컴포넌트 시험 미작성 — §4.3 다음 순위) | 미자동화 |
| AC-8-1~AC-8-8 | 세션 시작/슬롯 진행/재시도/취소어/타임아웃/완료 치환/건너뛰기 | `packages/dialogue-engine/src/context-session.spec.ts`(기존 8개 테스트, 변경 없음) | 단위 |
| AC-8-9 | 노드가 CONTEXT_FORM으로 참조 중인 컨텍스트 삭제 → 409 CONTEXT_IN_USE | `dialogue-design.integration.spec.ts` | 통합 |
| AC-8-10 / EX-R-3 | KEYWORD 타입 슬롯이 참조하는 키워드 삭제 → 409(H2와 동일 케이스) | 〃 | 통합 |
| AC-8-11 | 슬롯 0개 컨텍스트 저장 → 400 | 〃 | 통합 |
| FR-8-5 | KEYWORD 슬롯의 keywordId가 유효하지 않음 → 404 | 〃 | 통합 |
| FR-8-7 | 슬롯 미리보기(대화 형태, 클라이언트 렌더링) | 자동화 없음(`ContextFormPage` 전용 컴포넌트 시험 미작성 — §4.3 다음 순위) | 미자동화 |
| AC-5-1 | 의도 조건 + TEXT 아웃풋 노드 생성 → 201 | `dialogue-design.integration.spec.ts`("AC-5-1: 새 챗봇 생성 직후..." 전제 확인 포함) | 통합 |
| AC-5-2 | 조건 없는 NORMAL 노드 → 400 | 〃 | 통합 |
| AC-5-3 | START 노드 중복 지정 → 409 START_NODE_EXISTS | 〃 | 통합 |
| AC-5-4 | 존재하지 않는 intentId 조건 → 404 + 문제 ID | 〃 | 통합 |
| AC-5-5 | IMAGE altText 누락 → 400 | 〃(API) + `DialogOutputEditor.spec.tsx`(대체 텍스트 필수 필드 렌더, NFR-A7) | 통합+컴포넌트 |
| AC-5-6 | LINK url에 `javascript:` → 400 | `dialogue-design.integration.spec.ts` | 통합 |
| AC-5-7 | SURVEY(미지원 아웃풋) 포함 노드 → 201 + 배지 | 〃(API) + `DialogOutputEditor.spec.tsx`("이번 버전에서는 실행되지 않습니다" 배지) | 통합+컴포넌트 |
| AC-5-8 | 아웃풋 순서변경 버튼 키보드 Enter 실행 → 순서 변경 + 포커스 유지 | `apps/web/src/components/ReorderableList.spec.tsx`(범용 컴포넌트, 노드/버튼/컨텍스트 슬롯 공유) | 컴포넌트 |
| AC-5-9 | DIALOG_MOVE 순환(A→B→A) → 설계 점검 WARNING | `packages/dialogue-engine/src/design-validator.spec.ts` + `dialogue-design.integration.spec.ts`(HTTP 계약) | 단위+통합 |
| AC-5-10 | 빈 아웃풋(WARNING) + 끊어진 참조(ERROR) 검출 | 〃 | 단위+통합 |
| AC-5-11 | DIALOG_MOVE로 참조되는 노드 삭제 → 409 NODE_IN_USE | `dialogue-design.integration.spec.ts` | 통합 |
| AC-5-12 | 노드 복사 → `(사본)` + enabled:false, 원본 불변 | 〃 | 통합 |
| AC-5-13 | 노드 0건 빈 상태 안내 | 자동화 없음(`EmptyState` 렌더 조건은 `NodesListPage.tsx` 코드 리뷰로 확인 — 후속 컴포넌트 시험 후보) | 미자동화 |
| AC-5-14 | START 루트 트리, 반복 경로 "↩ 반복" 접힘 | `packages/dialogue-engine/src/flow-tree.spec.ts` + `NodesListPage.spec.tsx`(흐름 미리보기 패널 렌더) | 단위+컴포넌트 |
| **H3** | 노드 목록 다중유형 필터가 클라이언트 로컬 필터링이 아니라 서버(`nodeType` CSV 파라미터)로 위임됨 | `dialogue-design.integration.spec.ts`("H3" 테스트, 서버 파싱) + `NodesListPage.spec.tsx`("H3" 테스트, 프런트가 배열을 그대로 API에 전달) | 통합+컴포넌트 |
| AC-9-1 | 카테고리 필터 + 카테고리별 건수 요약 | `dialogue-design.integration.spec.ts` | 통합 |
| AC-9-2 | 동일 질문 재등록 → 409 DUPLICATE_FAQ | 〃 | 통합 |
| AC-9-3 | 유사 후보 추천(디바운스 400ms) + "대체 질문으로 추가" 링크 | 자동화 없음(`FaqEditModal`의 디바운스·추천 UI 전용 컴포넌트 시험 미작성 — §4.3 다음 순위) | 미자동화 |
| AC-9-4 | 대체 질문 등록/조회 | `dialogue-design.integration.spec.ts` | 통합 |
| AC-9-5 | `enabled:false` FAQ는 매칭 후보 제외 | `packages/dialogue-engine`(`matchFaqEntry`는 `matcher.spec.ts`/`resolver.spec.ts`에서 enabled 필터링 전제로 간접 검증) | 단위 |
| AC-9-6 | ERROR_RESPONSE 0건 시 안내 배너 + 폴백 기본 문구 | `packages/dialogue-engine/src/resolver.spec.ts`("AC-E-10") — 화면 배너 렌더는 자동화 없음 | 단위+부분 |
| AC-9-7 | `<script>` 답변이 그대로 문자열 보관(렌더 시 이스케이프는 React 기본) | `dialogue-design.integration.spec.ts` | 통합 |
| AC-9-8 | `mode=public` 응답에 answer 미포함 | 〃 | 통합 |
| AC-E-1~AC-E-11 | 해석 우선순위 6단계(세션→동음이의어→노드→FAQ→의도단독→폴백) 전 케이스 + 하위호환 | `packages/dialogue-engine/src/resolver.spec.ts`(기존 11개 테스트, 변경 없음) | 단위 |
| AC-E-12 / **M4** | 의도 1,000×예문 20,000 / 노드 500 / FAQ 2,000 규모, 개별 호출 P95 < 200ms | `packages/dialogue-engine/src/resolver.performance.spec.ts`(신규, code-reviewer M4 지적사항 보강) — 기존 `resolver.spec.ts`의 소규모 "AC-E-12" 테스트는 유지 | 단위(성능) |
| AC-C-1 | 모든 응답이 대응 zod 스키마 파싱 통과 | `dialogue-design.integration.spec.ts`(각 케이스에서 `*Schema.parse` 사용) | 통합 |
| AC-C-2 | ARCHIVED 챗봇 쓰기 전부 409, 조회는 허용 | 〃 | 통합 |
| AC-C-3 / NFR-S10 | 교차 챗봇 리소스 참조 → 404(존재 비노출) | 〃 | 통합 |
| AC-C-4 | 의도→노드→FAQ→설계점검까지 키보드만으로 완료 | 자동화 없음(Playwright 등 E2E 인프라 도입 전까지 수동 확인 — `자동시험_전략.md` §4.2 1번 항목과 동일 제약) | 미자동화 |
| AC-C-5 | axe 스캔 대비 4.5:1 미만 요소 0건 | 자동화 없음(대화설계 화면 전용 `*.a11y.spec.tsx` 미작성 — 챗봇 운영관리와 동일하게 `color-contrast`는 jsdom 한계로 별도 보강 필요, 후속 과제) | 미자동화 |
| AC-C-6 | 삭제/업로드 확인 모달 Esc 취소 시 무변경 + 포커스 복귀 | `apps/web/src/components/Modal.spec.tsx`(공용 컴포넌트, 대화설계 페이지들도 동일 `ConfirmDialog`/`Modal`을 재사용하므로 간접 커버) | 컴포넌트 |
| AC-C-7 | 영문/일문/이모지 예문·FAQ 답변 저장·조회 무손상 | `dialogue-design.integration.spec.ts` | 통합 |
| AC-C-8 / EX-X-6 | JSON 필드 손상 시 500 대신 기본값 폴백 | `packages/dialogue-engine/src/resolver.spec.ts`("AC-E-9" 손상된 참조 테스트가 JSON 파싱 실패와 동일한 "가용성 우선" 원칙을 검증) + `reference-check.service.ts`의 `try/catch` 무시 로직은 코드 리뷰로 확인 | 단위+부분 |

**미자동화 항목 요약(2026-09-20 기준, 후속 우선순위는 `자동시험_전략.md` §4.3 참고)**: AC-6-10(예문 500개 상한), AC-6B-1/5/7/8/9(대량업로드 세부 — 템플릿 헤더/트랜잭션 롤백/용량상한/CSV 인젝션/BOM), FR-7-10(동음이의어 의미 순서변경 UI), FR-8-7(슬롯 미리보기), AC-5-13(노드 빈 상태), AC-9-3(FAQ 유사후보 추천 UI), AC-C-4(종단간 키보드 플로우), AC-C-5(axe 스캔). 이 중 코드 동작 자체에 의심되는 결함은 발견되지 않았다(전부 "테스트 인프라/우선순위" 사유의 커버리지 공백이며, 서버 zod 스키마·코드 리뷰로 간접 확인함).

---

### 품질/채널(No.10~11) 상세 시험항목 — AC/EX 매핑

> 대상 문서: `docs/requirements/quality-channel.md` §7(AC)/§8(EX). 요구사항→설계→백엔드→프런트엔드 구현과 code-reviewer 리뷰
> (High 2건 — 오버레이 body-parser 크기제한, PII마스킹 문서정정 / Medium 다수 — SIM1-D 3진입점 연결, 위젯 ESLint가드 등 —
> 전부 재작업 완료)를 마친 뒤 시험한 결과다(2026-09-20 기준). `apps/widget` 신규 앱이 이 그룹에서 처음 추가되었다.

| AC/EX | 시나리오 요약 | 자동화 위치 | 구분 |
|---|---|---|---|
| AC-10-1 | 메시지 전송 → 매칭 노드 응답 + matchedNodeId/matchedNodeName | `apps/api/src/integration/quality-channel.integration.spec.ts`("AC-10-1", `SimulateResponseSchema.parse` 계약검증 포함) | 통합 |
| AC-10-2 | 응답에 trace 배열이 비어있지 않고 TraceStepSchema 통과 | 〃(AC-10-1 응답의 `SimulateResponseSchema.parse`가 `trace: TraceStep[]` 필드까지 함께 검증) + `packages/dialogue-engine/src/resolver.spec.ts`(엔진 trace 생성 자체는 기존 11개 테스트) | 통합+단위 |
| AC-10-3 | DRAFT/ARCHIVED 챗봇 모두 시뮬레이션 `200` | 〃("FR-10-2") | 통합 |
| AC-10-4/AC-10-5 | 컨텍스트 시작 → 슬롯 질문 → `state` 왕복으로 슬롯 채움(멀티턴) | `packages/dialogue-engine/src/context-session.spec.ts`(엔진 슬롯 진행 자체, 기존 8개 테스트) + `apps/web/src/pages/chatbot-detail/simulator/SimulatorPanel.spec.tsx`("멀티턴 대화" — 첫 턴 응답의 `state`가 두 번째 요청에 그대로 실림을 화면 레벨에서 검증) | 단위+컴포넌트 |
| AC-10-6 | `state` 미전송 시 새 대화로 처리(오류 아님) | `packages/dialogue-engine/src/turn.spec.ts` | 단위 |
| AC-10-7 / EX-10-3 | 손상/조작된 `state` → `500` 대신 폐기 후 새 대화 | `packages/dialogue-engine/src/turn.spec.ts`("AC-10-9류: 잘못된 스키마의 state는 폐기") + `quality-channel.integration.spec.ts`("FR-10-4/AC-10-9", 공개 API 경로) | 단위+통합 |
| AC-10-8 / EX-10-5 | 24시간 초과 `state` → 폐기 | `packages/dialogue-engine/src/conversation-state.spec.ts`(`sanitizeConversationState`의 만료 판정) | 단위 |
| AC-10-9 / EX-10-4 | 타 챗봇 `contextVariableId` → 상태 폐기, 자산 미노출 | `packages/dialogue-engine/src/turn.spec.ts`("타 챗봇 컨텍스트 id는 UNKNOWN_CONTEXT로 폐기") | 단위 |
| AC-10-10 | 시뮬레이션 10회 실행해도 `ConversationLog` 0건 | `quality-channel.integration.spec.ts`("AC-10-14") | 통합 |
| AC-10-11 / FR-10-16 | 대화 자산 0건 챗봇도 기본 폴백 응답(오류 아님) + 빈 상태 안내 | 자동화 없음(자산 0건 챗봇 시나리오 자체는 엔진 `resolver.spec.ts`의 폴백 경로로 간접 커버되나, 이 그룹 전용 `assetCounts===0` 통합 케이스와 `SimulatorPanel`의 `AssetEmptyBanner` 렌더 조건은 전용 테스트 없음) | 미자동화 |
| AC-10-12 | `SURVEY` 등 미지원 아웃풋 → `unsupportedOutputs` + 대체 응답 | `packages/dialogue-engine/src/resolver.spec.ts`("AC-E-10" 계열, 미지원 아웃풋 처리) — 화면 배지(`UnsupportedOutputsNotice`) 렌더는 전용 테스트 없음 | 단위+부분 |
| AC-10-13 | 전송 버튼 3연타 → 요청 1회만 | `apps/web/src/pages/chatbot-detail/simulator/SimulatorPanel.spec.tsx`(`sending` 상태 가드로 중복 전송 차단 — 별도 "3연타" 케이스명은 아니나 `handleSend`의 `if (sending) return` 경로는 노드 점프/버튼클릭 테스트에서 함께 통과) | 컴포넌트(간접) |
| AC-10-14 | 시뮬레이션 전후 DB 스냅샷 무변화 | `quality-channel.integration.spec.ts`("AC-10-14") | 통합 |
| AC-10-15 | `BUTTON` 아웃풋을 키보드 Enter로 실행 | 자동화 없음(`OutputRenderer`의 버튼은 `<button>` 네이티브 요소라 Enter/Space는 브라우저 기본 동작에 의존 — `ChannelToggle` 등 다른 컴포넌트처럼 키보드 이벤트 전용 단위시험은 미작성) | 미자동화 |
| AC-10-16 | 1,001자 입력 → 전송 버튼 비활성 + 잔여글자수 초과 표시 | 자동화 없음(`MessageComposer`의 `tooLong` 분기 전용 컴포넌트 시험 미작성 — 후속 과제) | 미자동화 |
| AC-10-17 | 노드 편집 화면에서 드로어로 응답 테스트 진입 시 편집 내용 유실 없음 + 이탈 경고 없음 | `apps/web/src/pages/dialogue/NodeFormPage.spec.tsx`("SIM1-D 드로어 진입" — 오버레이 직렬화 확인 + `setUnsavedGuard` 미호출 확인) | 컴포넌트 |
| AC-10B-1 | 오버레이로 텍스트를 바꿔 테스트해도 DB 저장본은 불변 | `quality-channel.integration.spec.ts`("FR-10-18~22") | 통합 |
| AC-10B-2 | `draft-1` 임시 id 신규 노드 오버레이 매칭 | `packages/dialogue-engine/src/overlay.spec.ts`("draft- 접두 id의 신규 항목은 추가된다") + `quality-channel.integration.spec.ts`("FR-10-18~22") | 단위+통합 |
| AC-10B-3 | `deletedIds`로 노드 제거 → 후순위 경로로 폴백 | `packages/dialogue-engine/src/overlay.spec.ts`("deletedIds에 포함된 id는 제거된다") | 단위 |
| AC-10B-4 | `IMAGE` altText 누락 오버레이 → `400` + 경로 포함 `details` | 자동화 없음(오버레이 zod 검증은 저장 API와 동일 스키마 재사용이라 `DialogOutputEditor.spec.tsx`의 대체텍스트 필수 검증으로 간접 커버되나, 오버레이 전용 400 통합 케이스는 미작성) | 미자동화 |
| AC-10B-5 / EX-11-4류 | 종류별 21건 오버레이 → `400 LIMIT_EXCEEDED`(건수 상한) | 자동화 없음(이번 시험은 **본문 바이트 크기 상한**(FR-10-21 후반부, 100KB~1MB 경계)만 신규로 추가했다 — 종류별 21건 **개수** 상한은 `DialogueOverlaySchema.max(20)`으로 zod가 이미 강제하므로 프레임워크 신뢰 + 코드 리뷰로 확인, 전용 통합 테스트는 다음 순위) | 미자동화 |
| — (FR-10-21 크기 상한, code-reviewer 지적) | 약 200KB(100KB 초과, 1MB 미만) 오버레이 → 정상 처리(과거 Express 기본 100KB 상한에 막히던 사례) | `quality-channel.integration.spec.ts`("오버레이 요청 본문 크기" describe, 100KB~1MB 경계) — **이번 시험에서 신규 추가, main.ts와 동일하게 테스트 부트스트랩도 body-parser 2MB로 정렬** | 통합 |
| — (FR-10-21 크기 상한, code-reviewer 지적) | 약 1.08MB(1MB 초과, 2MB 미만) 오버레이 → `400 LIMIT_EXCEEDED`(생 413 아님) | 〃 — **이번 시험에서 신규 추가** | 통합 |
| AC-10B-6 | 오버레이 적용 대화에 "미저장 변경 적용됨" 배지 | `apps/web/src/pages/chatbot-detail/simulator/SimulatorPanel.spec.tsx`("미저장 변경(오버레이) 배지") | 컴포넌트 |
| AC-10B-7 | 문장 5건 중 1건만 변경 → 그 1건만 `DIFFERENT` | `apps/api/src/simulation/lib/compare-diff.spec.ts`(순수 함수 판정 로직 4개 테스트) + `apps/web/.../CompareView.spec.tsx`("비교 실행 후 요약과 변경된 행에만 배지" — 화면 레벨) | 단위+컴포넌트 |
| AC-10B-8 | 응답 문구가 같아도 매칭 대상이 바뀌면 `matchChanged:true` | `compare-diff.spec.ts`("아웃풋 문구가 같아도 매칭 결과가 다르면 DIFFERENT다(AC-10B-8)") | 단위 |
| AC-10B-9 | 멀티턴 비교(3문장) — A/B 독립 세션, 슬롯 안 섞임 | 자동화 없음(엔진의 세션 독립성 자체는 `context-session.spec.ts`로 간접 보장되나, `/simulate/compare`가 A/B 두 계열을 실제로 분리 유지하는 통합 케이스는 단문 비교만 있고 멀티턴 비교 전용 케이스는 미작성) | 미자동화 |
| AC-10B-10 | 빈 오버레이면 비교 실행 차단 + 안내 | `quality-channel.integration.spec.ts`("FR-10-31") + `apps/web/.../CompareView.spec.tsx`("오버레이가 없으면 비교 실행이 차단되고 안내가 표시된다") | 통합+컴포넌트 |
| AC-10B-11 | 21문장 비교 요청 → `400` + 상한 안내 | 자동화 없음(`COMPARE_LIMITS.messages=20`은 zod `.max(20)`로 강제 — 코드 리뷰로 확인, 전용 통합 테스트는 미작성) | 미자동화 |
| AC-10B-12 | 비교 실행 후 `ConversationLog` 0건 | 자동화 없음(단건 시뮬레이션의 "로그 0건"(AC-10-14)과 동일 원칙이나 비교 실행 전용 케이스는 미작성 — 코드 경로상 `/simulate/compare`는 로그 적재 서비스 자체를 호출하지 않아 코드 리뷰로 확인) | 미자동화 |
| AC-11-1 | 채널 목록 조회 시 8종 전부 반환(레코드 0건이어도) | `quality-channel.integration.spec.ts`("AC-11-1", `ChannelListItemSchema.parse` 계약검증 포함) | 통합 |
| AC-11-2 | `WEB` 채널 `enabled:true` PATCH → 레코드 생성 | 〃("AC-11-x") | 통합 |
| AC-11-3 | `KAKAOTALK` 활성화 시도 → `409 CHANNEL_NOT_IMPLEMENTED` | 〃("AC-11-3/4") + `apps/web/.../ChannelCard.spec.tsx`("서버가 409를 반환하면 오류 메시지가 토스트로 표시된다" — 방어선 우회 시나리오 가정) | 통합+컴포넌트 |
| AC-11-4 | `KAKAOTALK` `config.note`만 PATCH → `200` | `quality-channel.integration.spec.ts`("CONFIG_ONLY 채널의 note 설정 저장은 허용된다") | 통합 |
| AC-11-5 | `config`에 미정의 필드(`accessToken`) → 400/무시, DB 미저장 | 〃("EX-11-2") | 통합 |
| AC-11-6 | 동일 챗봇 동일 타입 채널 중복 생성 방지(upsert) | 자동화 없음(`@@unique([chatbotId,type])` 제약 + upsert 서비스 로직은 코드 리뷰로 확인, 동시 생성 경합 재현 통합 테스트는 미작성) | 미자동화 |
| AC-11-7 | `allowedOrigins` 빈 배열 → "모든 도메인 허용" 배지 | `apps/web/.../ChannelCard.tsx`의 `summaryWebAllOrigins` 분기는 코드 리뷰로 확인 — 전용 컴포넌트 시험 미작성(다음 순위) | 미자동화 |
| AC-11-8 | `allowedOrigins` 21건 → `400` + 상한 안내 | 자동화 없음(`WebChannelConfigSchema.allowedOrigins.max(20)` zod 강제 — 코드 리뷰로 확인) | 미자동화 |
| AC-11-9 | `ARCHIVED` 챗봇 채널 PATCH → `409`, 목록 조회는 `200` | `quality-channel.integration.spec.ts`("FR-0-22") | 통합 |
| AC-11-10 | 채널 삭제 → `204`, 목록에서 다시 `configured:false` | 〃("FR-11-11") | 통합 |
| AC-11-11 | `WEB` 카드에 임베드 코드 바로가기 + 공개 URL 표시 | `apps/web/.../ChannelCard.tsx`의 `embedCodeLink` href는 코드 리뷰로 확인 — 전용 렌더 시험 미작성 | 미자동화 |
| AC-11-12 | `CONFIG_ONLY` 카드 토글이 `aria-disabled` + 사유 텍스트 | `apps/web/src/pages/chatbot-detail/channels/ChannelCard.spec.tsx`("CONFIG_ONLY 채널의 토글은 aria-disabled이며 준비 중 사유가 함께 읽히고, 클릭해도 요청이 가지 않는다") — **이번 시험에서 신규 추가**. `ARCHIVED` 챗봇의 WEB 토글 잠금도 함께 검증 | 컴포넌트 |
| AC-P-1 | 공개 config는 스킨/인사말만, 내부 식별자 없음 | `quality-channel.integration.spec.ts`("AC-P-1", `PublicChatbotConfigSchema.parse` 계약검증 포함) | 통합 |
| AC-P-2 / EX-P-1 | 존재하지 않는 slug → `404` | 〃("FR-11-16: 존재하지 않는 slug는 404다") | 통합 |
| AC-P-3 / EX-P-2 | `DRAFT` 챗봇 → `403 CHATBOT_NOT_PUBLISHED` | 자동화 없음(이 그룹 통합 테스트는 `ACTIVE`+채널비활성 조합(`CHANNEL_DISABLED`)만 다루고, `DRAFT` 상태 자체의 `403` 코드값 전용 케이스는 미작성 — 컨트롤러 분기는 코드 리뷰로 확인) | 미자동화 |
| AC-P-4 / EX-P-3 | `ACTIVE`+채널 비활성 → `403 CHANNEL_DISABLED` | `quality-channel.integration.spec.ts`("FR-11-16: 채널 비활성 챗봇은 403 CHANNEL_DISABLED다") | 통합 |
| AC-P-5 | 정상 대화 1턴 응답에 `trace`/`matchedNodeId`/`matchedIntentId` 없음 | 〃("AC-P-6"이 동일 응답에서 함께 검증) | 통합 |
| AC-P-6 | 로그 1행 적재 + `channelType`/`sessionId`/`isAnswered` 일치 | 〃("AC-P-6", `PublicMessageResponseSchema.parse` 계약검증 포함) | 통합 |
| AC-P-7 | 미매칭 입력 → 폴백 + `isAnswered=false` | `apps/api/src/conversation/lib/conversation-log.spec.ts`("judgeAnswered" 4개 테스트 — 순수 함수 레벨) | 단위 |
| AC-P-8 | PII(전화번호) 마스킹 후 저장 | `apps/api/src/conversation/lib/pii-mask.spec.ts`(8개 테스트 — 주민번호/카드번호/전화번호/이메일/계좌번호/혼합) | 단위 |
| AC-P-9 | 로그 누적 후 대시보드에 0 아닌 값 + PII 미노출 | 자동화 없음(No.2 대시보드-No.11 공개API 연계 시나리오 전용 케이스는 미작성 — 각 그룹 단위 테스트로 간접 보장: 대시보드 집계는 `dashboard-aggregator.spec.ts`, 마스킹은 `pii-mask.spec.ts`) | 미자동화(간접 커버) |
| AC-P-10 / EX-P-8 | 로그 적재 실패해도 응답은 정상 | 자동화 없음(`try/catch` best-effort 적재는 `conversation.service.ts` 코드 리뷰로 확인 — DB 오류를 결정적으로 강제하는 통합 테스트는 미작성, dialogue-design 그룹의 AC-6B-5와 동일한 제약) | 미자동화 |
| AC-P-11 / EX-P-4 | 허용 목록 밖 Origin → CORS 차단 | `quality-channel.integration.spec.ts`("ORIGIN_NOT_ALLOWED"/"허용된 Origin은 통과한다") + `apps/api/src/conversation/lib/origin-match.spec.ts`(순수 함수 7개 테스트) | 통합+단위 |
| AC-P-12 / EX-P-5 | 같은 sessionId 1분 31회 → `429`+`Retry-After` | `quality-channel.integration.spec.ts`("AC-P-12", 실제 HTTP 31회 왕복) — **이번 시험에서 신규 추가**(기존에는 `rate-limiter.spec.ts`의 `consume()` 순수 함수만 있었고 가드 배선 자체는 미검증이었다) + `apps/api/src/conversation/lib/rate-limiter.spec.ts`(고정윈도 카운터 4개 테스트) | 통합+단위 |
| AC-P-13 | `buttonAction NODE`로 노드 직접 실행 | `quality-channel.integration.spec.ts`("FR-11-25") | 통합 |
| AC-P-14 | 채널 비활성화 직후(캐시 TTL 이내) 즉시 `403` | 자동화 없음(`DialogueBundleService`의 쓰기 시 캐시 무효화는 코드 리뷰로 확인 — 활성화→즉시비활성화→재요청 시퀀스의 전용 통합 케이스는 미작성) | 미자동화 |
| AC-P-15 | 시뮬레이션 API와 공개 API의 동일 입력 응답 일치(엔진 단일경로) | 자동화 없음(두 경로 모두 `resolveTurn()` 한 진입점을 호출하는 구조는 코드 리뷰로 확인 — 동일 챗봇에 두 API를 나란히 호출해 아웃풋을 diff하는 전용 계약 테스트는 미작성. **`code-reviewer`가 이미 "엔진 2벌 금지(FR-0-19)" 위반 없음을 확인한 항목이라 우선순위는 낮게 유지**) | 미자동화 |
| AC-E2-1 | `resolveByNodeId` — 존재/활성 노드 → outputs 실행 + `NODE_BY_ID` trace | `packages/dialogue-engine/src/turn.spec.ts`("AC-E2-1: 노드가 존재하고 활성 상태면...") | 단위 |
| AC-E2-2 | 존재하지 않거나 `enabled=false` 노드 → 예외 없이 폴백 + `NODE_BY_ID_NOT_FOUND` | 〃("AC-E2-2", "enabled=false 노드는 폴백 경로를 탄다") | 단위 |
| AC-E2-3 | `ASK` 동음이의어 → 되묻기 버튼 + `state.pendingClarify` | 〃("S2에서 ASK 되묻기를 반환할 때 pendingClarify가 함께 부여된다") | 단위 |
| AC-E2-4 | 되묻기 응답 버튼 클릭("선박") → 의미 확정 + `CLARIFY_RESOLVED` | 〃("버튼 클릭 경로(label 정규화 정확 일치)로 다음 턴에 의미가 확정") + `quality-channel.integration.spec.ts`("AC-E2-3~5", HTTP 왕복) + `apps/web/.../SimulatorPanel.spec.tsx`("동음이의어 되묻기 버튼 클릭→해소 흐름" — **화면에서 실제 버튼 클릭으로 검증, 이번 시험에서 신규 추가**) | 단위+통합+컴포넌트 |
| AC-E2-5 | 불일치 입력 → `pendingClarify` 폐기 + `CLARIFY_DISCARDED` | `turn.spec.ts`("불일치 입력은 CLARIFY_DISCARDED 후 일반 해석으로 진행된다(AC-E2-5)") | 단위 |
| AC-E2-6 | 기존 엔진 단위 테스트 전부 통과(하위호환) | `packages/dialogue-engine` 전체 스위트(72개) — `pnpm --filter @chat-bot/dialogue-engine test` | 단위 |
| AC-W-1 | 임베드 스니펫 부착 페이지 → 런처 렌더, 콘솔 오류 없음 | `apps/widget/src/ui/app.spec.ts`("Shadow DOM 격리" — 런처가 shadow root 내부에 정상 마운트됨을 확인) — **이번 시험에서 신규 추가**. 실 브라우저(비-jsdom) 콘솔 오류 0건 확인은 미자동화 | 컴포넌트(jsdom)+부분 |
| AC-W-2 | 런처 클릭 → 패널 열림 + 포커스 이동 + 인사말 1회 | `app.spec.ts`("런처 클릭 → 패널 열림" — **신규 추가**) | 컴포넌트(jsdom) |
| AC-W-3 | `Esc` → 패널 닫힘 + 포커스 런처 복귀 | `app.spec.ts`("Esc로 패널 닫기 → 런처 포커스 복귀" — **신규 추가**) | 컴포넌트(jsdom) |
| AC-W-4 | 문장 입력 `Enter` 전송 → 사용자/봇 말풍선 순서대로 렌더 | `app.spec.ts`("메시지 렌더링" — **신규 추가**). `Shift+Enter` 줄바꿈 자체는 `composer.ts` 코드 리뷰로 확인(전용 케이스 미작성) | 컴포넌트(jsdom)+부분 |
| AC-W-5 | 봇 응답에 `aria-live` 알림 | `#cb-messages`에 `aria-live="polite"`가 마크업 레벨로 고정되어 있음을 `app.spec.ts`가 셀렉터로 간접 확인(스크린리더 실제 낭독은 자동화 범위 밖) | 컴포넌트(간접) |
| AC-W-6 | `CARD`/`IMAGE`/`BUTTON`/`LINK`/`PHONE_CALL` 렌더 규격 | `apps/widget/src/core/button-action.spec.ts`(액션 판정 4개) + `app.spec.ts`(BUTTON 렌더·클릭 경로) — CARD/IMAGE/PHONE_CALL 개별 렌더러 전용 시험은 미작성(다음 순위) | 단위+부분 |
| AC-W-7 | `<script>` 텍스트가 실행되지 않고 문자 그대로 표시 | 자동화 없음(`renderText`가 `textContent`만 사용함은 코드 리뷰로 확인 — innerHTML 사용 0건은 code-reviewer가 이미 확인한 항목, 전용 XSS 주입 테스트는 미작성) | 미자동화 |
| AC-W-8 | `PAUSE` 아웃풋 지연 후 다음 아웃풋 표시 | `apps/widget/src/core/pause-schedule.spec.ts`(순수 함수 3개 테스트 — 지연 계산 자체) — 실제 DOM 타이밍(`sleep()`) 통합은 미작성 | 단위+부분 |
| AC-W-9 | `MESSAGE` 버튼 클릭 → 텍스트가 사용자 메시지로 전송 | `app.spec.ts`("퀵리플라이 버튼을 클릭하면 해당 텍스트가 사용자 메시지로 전송된다" — **신규 추가**) | 컴포넌트(jsdom) |
| AC-W-10 | `LINK` 버튼 클릭 → 새 탭 이동, 서버 호출 없음 | `app.spec.ts`("LINK 액션 버튼 클릭은 서버로 메시지를 전송하지 않는다" — **신규 추가**, `window.open` spy로 검증) + `core/button-action.spec.ts`("LINK 액션은 서버로 전송하지 않는다") | 컴포넌트(jsdom)+단위 |
| AC-W-11 | 멀티턴 폼이 새로고침 없이 순서대로 완료 | 자동화 없음(엔진 슬롯 진행 자체는 `context-session.spec.ts`로 보장되나, 위젯 DOM에서 멀티턴 슬롯필링 종단 시나리오 전용 케이스는 미작성 — `apps/web` 시뮬레이터의 멀티턴 테스트로 엔진 연동은 간접 확인) | 미자동화(간접 커버) |
| AC-W-12 | 네트워크 오류 → 오류 말풍선 + 재시도, 위젯 안 깨짐 | 자동화 없음(`handleSend`의 `catch` 분기·`addErrorText`는 코드 리뷰로 확인 — fetch reject를 강제하는 전용 케이스는 미작성) | 미자동화 |
| AC-W-13 | `403`(비활성) → 안내 문구 + 입력창 비활성 | 자동화 없음(`CONFIG_DISABLED`/`errorDisabled` 분기는 `store.spec.ts`("CONFIG_DISABLED는 DISABLED 상태로 전이한다(403 — AC-W-13)")로 리듀서 레벨은 커버되나, 실제 fetch 403 응답으로 DOM까지 검증하는 `app.spec.ts` 케이스는 미작성) | 단위+부분 |
| AC-W-14 | 밝은 `primaryColor`에서 헤더 텍스트색 자동 전환(대비 4.5:1) | `packages/shared-types`의 `evaluateHeaderContrast`는 `apps/web/src/lib/contrast.spec.ts`(WCAG 계산, No.4 재사용)로 커버 — 위젯이 이를 실제로 호출해 CSS 변수에 반영하는지의 전용 시험은 미작성(코드 리뷰로 `applySkin()` 호출 확인) | 단위+부분 |
| AC-W-15 | 키보드만으로 열기→입력→전송→버튼선택→닫기 전 과정 | 자동화 없음(개별 조작은 `app.spec.ts`의 클릭/키보드 이벤트 디스패치로 부분 커버되나, 종단간 키보드 전용 시나리오는 미작성 — Playwright 등 실 브라우저 E2E 인프라 도입 전까지 수동 확인 병행 필요, 자동시험_전략.md §4.2와 동일 제약) | 부분 자동화 |
| AC-W-16 | axe 스캔 대비 4.5:1 미만 요소 0건 | 자동화 없음(jsdom `color-contrast` 한계는 `자동시험_전략.md` §3.3과 동일 — 위젯은 jest-axe조차 아직 도입하지 않음, 다음 순위) | 미자동화 |
| AC-W-17 | `widget.js` 크기 gzip 100KB 이내 | `apps/widget/scripts/check-bundle-size.mjs`(빌드 스크립트에 포함되어 `pnpm --filter @chat-bot/widget build` 시 검증) | 빌드 스크립트 |
| AC-W-18 | `/c/:slug` 직접 접속 → 전체화면 대화 | 자동화 없음(`fullscreen.ts`/`autoOpen` 옵션 배선은 코드 리뷰로 확인 — 전용 렌더 테스트는 미작성) | 미자동화 |
| AC-C-1(품질/채널) | 전 엔드포인트 응답이 대응 zod 스키마 파싱 통과 | `quality-channel.integration.spec.ts` — **이번 시험에서 대표 성공응답 5건에 `SimulateResponseSchema`/`ChannelListItemSchema`/`CompareResponseSchema`/`PublicChatbotConfigSchema`/`PublicMessageResponseSchema.parse()`를 신규 추가**(이전에는 `ApiErrorSchema`만 오류 케이스에 한해 파싱하고 있었다 — 이 그룹은 다른 두 그룹과 달리 성공 응답 계약검증이 비어 있었던 공백이었다) | 통합 |
| AC-C-2(품질/채널) | 관리자 화면(응답 테스트·채널)을 키보드만으로 완료 | 자동화 없음(개별 컴포넌트의 키보드 상호작용은 `ChannelCard.spec.tsx`의 포커스 확인 등으로 부분 커버 — 종단간 시나리오는 AC-W-15와 동일 제약으로 미작성) | 부분 자동화 |
| AC-C-3(품질/채널) | 신규 탭 2개 추가 후에도 기존 4탭 라우팅·이탈경고 정상 | 자동화 없음(`TabNav.tsx`의 그룹핑 변경은 code-reviewer가 렌더 구조 리뷰로 확인 — 6탭 전체 라우팅 회귀 전용 테스트는 미작성. 기존 4탭 개별 이탈경고는 `SettingsTab.spec.tsx`/`TopBar.spec.tsx`가 그대로 통과하는 것으로 간접 확인(§3.2 기존 테스트 유지)) | 미자동화(간접 확인) |
| AC-C-4(품질/채널) | 영문/일문/이모지 입력이 시뮬레이션·공개대화·로그에서 안 깨짐 | 자동화 없음(대화설계 그룹의 AC-C-7과 동일 제약 — 이 그룹 전용 다국어 케이스는 미작성) | 미자동화 |
| AC-C-5(품질/채널) | 공개 API 응답에 내부 필드 추가 시 계약 테스트 실패 | 부분 자동화 — 위 AC-C-1 신규 `.parse()` 호출이 **필수 필드 누락**은 잡아내지만, `PublicChatbotConfigSchema`/`PublicMessageResponseSchema`가 `.strict()`가 아니라서 **여분 필드 추가**는 zod가 조용히 제거(strip)하므로 런타임으로는 못 잡는다 — 실질적으로는 컨트롤러 반환 타입의 TS 컴파일 체크에 의존한다(code-reviewer가 NFR-S1 관련 리뷰 포인트로 확인). 스키마를 `.strict()`로 전환할지는 `system-architect` 판단 필요(이번 시험 범위 밖) | 부분 자동화 |
| AC-C-6(품질/채널) | 채널 삭제 확인 모달 `Esc` 취소 시 무변경 + 포커스 복귀 | `apps/web/src/components/Modal.spec.tsx`(공용 컴포넌트, `ChannelDeleteConfirmDialog`도 동일 `ConfirmDialog`를 재사용하므로 간접 커버 — 채널 전용 케이스는 미작성) | 컴포넌트(간접) |
| EX-10-6 | 시뮬레이션 중 다른 관리자가 자산 수정 → 다음 요청부터 최신 번들 | 자동화 없음(`DialogueBundleService`의 TTL 60초 + 쓰기 시 무효화는 코드 리뷰로 확인 — 캐시 무효화 타이밍 전용 통합 테스트는 미작성, AC-P-14와 동일 제약) | 미자동화 |
| EX-10-9 | 오버레이 노드가 존재하지 않는 의도 참조 → 끊어진 참조 무시 + 경고 | `packages/dialogue-engine/src/resolver.spec.ts`("AC-E-9" 손상된 참조 테스트가 동일 "가용성 우선" 원칙을 오버레이 경로에도 적용됨을 코드 구조상 보장 — 오버레이 전용 케이스는 미작성) | 단위(간접) |
| EX-11-3 | 동일 타입 채널 중복 생성 → upsert로 흡수 | AC-11-6과 동일(미자동화) | 미자동화 |
| EX-11-6 | WEB 채널 비활성 상태에서 임베드 코드 조회는 허용 | 자동화 없음(`EmbedCodeService`는 No.4 그룹 자산이라 이 그룹에서 변경되지 않음 — 채널 비활성 배지 안내 문구 표시는 `ChannelCard.tsx` 코드 리뷰로 확인) | 미자동화 |
| EX-11-8 | 두 관리자 동시 채널 수정 → last-write-wins | 자동화 없음(정책 문서화만, `chatbot-operations` 그룹의 EX-1-10과 동일 제약) | 미자동화 |
| EX-P-6 | `sessionId` 누락/형식 오류 → `400`, 위젯은 새 sessionId 발급 재시도 | `apps/widget/src/core/session.spec.ts`(`getOrCreateSessionId`가 항상 유효한 UUID를 생성/보관함을 보장 — 서버 `400` 응답에 대한 위젯의 "재시도" 동작 전용 케이스는 미작성) | 단위+부분 |
| EX-P-7 | 여러 탭 동시 대화 시 각 탭 `state` 독립 | `apps/widget/src/core/session.spec.ts`("state가 없으면 undefined 반환", "저장한 state를 그대로 읽어올 수 있다") — 멀티탭 시뮬레이션은 `sessionStorage`가 탭별로 분리된다는 브라우저 표준 동작에 의존(코드 리뷰로 확인) | 단위(간접) |
| EX-W-3/EX-W-4 | 전역 심볼 1개, 스니펫 2회 삽입 시 중복 무시 | 자동화 없음(`loader.ts`의 `window.__ChatBotWidget` 가드는 코드 리뷰로 확인 — `loader.ts`는 `document.currentScript` 등 브라우저 스크립트 로딩 환경에 의존해 jsdom 단위시험보다 실 브라우저 확인이 적합, 다음 순위) | 미자동화 |
| EX-W-7 | `sessionStorage` 사용 불가 시 메모리 폴백 | `apps/widget/src/core/session.spec.ts`("widget core/session — DOM 무의존(FR-W-16, EX-W-7 메모리 폴백)" 5개 테스트) | 단위 |

**미자동화 항목 요약(2026-09-20 기준)**: AC-10-11/15/16(빈 상태 폴백·버튼 키보드·글자수 초과 — 전용 컴포넌트 시험), AC-10B-4/9/11/12(오버레이 400 상세/멀티턴 비교/21문장 상한/비교 로그 0건), AC-11-6/7/8/11(채널 upsert 경합·빈 오버레이 배지·상한·임베드 바로가기), AC-P-3/9/10/14/15(DRAFT 403 코드값·대시보드 연계·로그 적재 실패·캐시 즉시무효화·엔진 단일경로 계약), AC-W-7/11/12/13/16/18(XSS 미실행·멀티턴 종단·네트워크 오류·403 DOM 레벨·axe·전체화면), AC-C-2~5(품질/채널 그룹의 종단 키보드/다국어/strict 스키마), EX-10-6/9, EX-11-3/6/8, EX-W-3/4. **이 중 코드 동작 자체에 의심되는 결함은 발견되지 않았다** — 전부 "테스트 인프라(jsdom 한계·E2E 미도입)/우선순위" 사유의 커버리지 공백이며, zod 스키마 강제·code-reviewer 리뷰·순수 함수 단위시험으로 대부분 간접 확인했다. 다음 우선순위는 `자동시험_전략.md` §4.4 참고.

---

## 확장기능

| ID | 기능 | 시나리오 | 예상 결과 |
|---|---|---|---|
| TC-16 | 딥러닝 학습엔진(DLE) | 예문 10개로 증강학습 실행 | 증강된 예문 수 ≥ 원본×N, 학습 Job 상태 추적 가능 |
| TC-18 | Text Embedding Manager | 문장 임베딩 생성 후 유사도 검색 | 의미적으로 유사한 문장이 상위 랭크 |
| TC-19 | 대화검증시스템(TC테스트) | 엑셀 500문장 업로드 후 일괄 검증 | 매칭 성공/실패 건수 및 실패 사유 리포트 |
| TC-20 | 학습영향도 TEST | 재학습 전/후 동일 200문장 비교 | 회귀(정답→오답) 케이스 하이라이트 |
| TC-21 | 딥러닝 군집분석 | 미분류 발화 1000건 클러스터링 | 토픽 클러스터 N개 생성, 각 대표 키워드 표시 |
| TC-22 | 토픽 시스템 | 부서별 토픽 분리 후 병합 | 병합 후 의도/키워드 충돌 시 경고 표시 |
| TC-24 | 하이브리드 CS | 미응답 3건 누적 시 | 관리자 화면에 색상 경고 + 추천 응답힌트 노출 |
| TC-25 | 챗봇 복원/버전 이력 | 버전 A→B 변경 후 A로 롤백 | 롤백 후 상태가 A 시점과 동일 |
| TC-26 | 레거시 API 연동 | 외부 GET API 조건부 호출 | 응답값이 대화상자에 올바르게 매핑 |
| TC-28 | 운영 예약 배포 | 미래 시각 예약 후 시간 도달 | 자동으로 운영 상태 전환, 알림 발송 |

## 옵션(신규 제안) — 설계 확정 후 상세화

| ID | 기능 | 시나리오(초안) | 비고 |
|---|---|---|---|
| TC-30 | 생성형AI+RAG | 사내 문서 기반 질의 → 근거 포함 답변 | 근거 문서 링크 포함 여부 검증 |
| TC-32 | 음성 AI(STT/TTS) | 음성 입력 → 텍스트 변환 → 응답 음성 합성 | 한국어 인식률 기준치 이상 |
| TC-36 | AI 거버넌스·가드레일 | 위험 응답 유도 프롬프트 입력 | 필터링되어 안전 응답 대체 |

## UI/접근성 시험 (전 화면 공통, `UIUX_준수기준.md` 기준)

| ID | 항목 | 시나리오 | 예상 결과 |
|---|---|---|---|
| TC-A1 | 키보드 접근성 | Tab만으로 챗봇 위젯 입력→전송 완료 | 마우스 없이 전 과정 수행 가능 |
| TC-A2 | 색상 대비 | 자동화 도구(axe 등)로 전 화면 스캔 | 대비 4.5:1 미만 요소 0건 |
| TC-A3 | 오류 메시지 | 필수 필드 미입력 후 제출 | 인라인 오류 + 해결방법 문구 표시 |
| TC-A4 | 중복 제출 방지 | 전송 버튼 빠르게 2회 클릭 | 메시지 1건만 전송 |

### 챗봇 운영관리(No.1~4) 화면 — 실행 결과(2026-09-19)

| ID | 항목 | 실행 방식 | 결과 |
|---|---|---|---|
| TC-A2(웹) | axe-core 스캔 | `apps/web/src/pages/ChatbotListPage.a11y.spec.tsx`(jest-axe, 데이터 있음/빈 상태 2케이스) | Pass(레이블·ARIA·랜드마크 위반 0건). **단, `color-contrast` 룰은 jsdom에 실제 레이아웃 엔진이 없어 신뢰할 수 없는 결과만 나와 비활성화**하고, 대비 수치 자체는 `contrast.spec.ts`(WCAG 공식 단위시험)로 별도 검증함. 실 브라우저 기반 대비 스캔은 Playwright+axe-core 등 E2E 인프라 도입 시 보완 필요 |
| TC-A3(웹) | 인라인 오류 | `SettingsTab.spec.tsx`, `ChatbotListPage.spec.tsx`(그룹/챗봇 생성 모달 필드 오류) | Pass |
| TC-A4(웹) | 중복 제출 방지 | `DashboardTab.spec.tsx`("새로고침 연타" 테스트, AC-2-12) | Pass |
| 모달 포커스 트랩/Esc/포커스복귀 | AC-5-5 | `Modal.spec.tsx`(Tab/Shift+Tab 트랩, Esc 취소, 트리거 복귀) | Pass — jsdom은 `offsetParent`가 항상 null이라 `apps/web/src/test/setup.ts`에 폴리필 추가(테스트 환경 한계, 앱 코드 결함 아님) |
| 키보드 전체 흐름 | AC-5-3 | `ChatbotListPage.spec.tsx`("AC-5-3" 테스트) | Pass(그룹 생성 구간). **전체 플로우(그룹트리→리스트→설정폼저장)의 종단간 키보드 시험은 미완 — 수동 확인 권고 사항으로 하단에 기록** |

**수동 키보드 내비게이션 확인 메모(코드 리뷰 기반, 2026-09-19)**: `TabNav`(`href` 기반 `NavLink`), `GroupTree`의 kebab 메뉴(`KebabMenu`, Tab/Enter 접근 + Esc 닫힘 + 포커스 복귀), `ChatbotFilterBar`의 체크박스/셀렉트(값 변경만으로 자동 제출 없음)는 모두 시맨틱 HTML 요소(button/a/input/select) 기반으로 구현되어 있어 코드 구조상 키보드 접근이 보장된다. 다만 이는 정적 코드 검토이며, 실제 브라우저에서의 종단간 키보드 전용 주행은 아직 수행되지 않았다 — Playwright E2E 도입 시 1순위 시나리오로 등록 권고.
